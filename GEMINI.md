# Workspace Rules (KISAPP)

## 브라우저 테스트 및 MCP 제어 규칙
- **세션 초기화 금지**: 새 브라우저 컨텍스트를 임의로 재시작하거나 첫 페이지로 리셋하지 말 것.
- **현재 열린 세션 유지**: 이미 열려 있는 탭/페이지가 있다면 새로고침이나 새 창 열기 없이 해당 화면 상태(URL, 로그인 세션)에서 작업을 이어갈 것.
- **단계별 확인**: 화면 상태가 예상과 다르면 임의로 다른 링크를 누르지 말고, 현재 화면 스크린샷이나 에러 상태를 보고하고 대기할 것.

## Firebase 프로젝트 격리 및 배포 안전 규칙
1. **프로젝트 ID 바인딩 확인**:
   - Firebase 관련 작업(보안 규칙 배포, Functions, Firestore 조회 등)을 수행할 때는 절대 MCP의 기본 세션 값에 의존하지 않는다.
   - 작업 시작 전 반드시 현재 워크스페이스의 `.firebaserc` 또는 `firebase.json`에서 정확한 `projectId`를 확인하고, `firebase_update_environment`를 통해 현재 프로젝트 디렉토리와 `active_project`를 1:1로 일치시킨다.
2. **배포 시 명시적 프로젝트 플래그 필수**:
   - CLI 명령어로 Firebase 배포(`deploy`)를 실행할 때는 반드시 `--project <프로젝트ID>` 플래그를 명시하여 타 프로젝트로의 오배포를 원천 차단한다.
3. **배포 승인 규칙**:
   - 사용자의 명시적인 배포 승인("배포해", "배포해줘")이 있을 때만 원격 푸시 및 배포 명령을 실행한다.

## 스쿨버스 및 방과후 도메인 로직 규칙
1. **경로 및 요일 엄격 필터링**:
   - 스쿨버스 목적지 파악, 미편성 목적지 계산, 탑승 학생 필터링 시 반드시 선택된 **경로(`routeType`: 등교/하교/방과후)**와 **요일(`dayOfWeek`)**에 실제로 탑승하는 학생만 대상으로 추출한다.
   - 특히 `AfterSchool`(방과후)의 경우, 해당 요일에 방과후 수업을 실제로 수강하거나 방과후 목적지가 등록된 학생만 대상으로 해야 하며, 수업이 없는 요일(금요일 등)에는 대상자가 0명으로 정확히 처리되어야 한다. 일반 하교 목적지(`afternoonDestinationId`)를 임의 fallback으로 사용하여 대량의 오탐 목적지가 발생하는 것을 원천 차단한다.
   - 해당 경로/요일의 노선(stops)에 이미 등록되어 있는 목적지(ID 및 명칭 매칭)는 미편성 목록에서 완벽하게 제외한다.

2. **관계형 참조 필드 유효 데이터 검증 (Orphan ID 누출 방지)**:
   - 버스 번호, 목적지 등 참조 필드를 렌더링할 때는 유효성 검증 없이 Firestore 문서 ID를 fallback으로 화면에 노출하지 않는다.
   - 반드시 실제로 등록된 유효 데이터(`b.name` 존재)만 필터링하고 번호순으로 정렬하여 사용자 화면에 유령 ID가 노출되는 것을 원천 차단한다.

3. **버스 배정 제외(Excluded) 설정의 경로 타입별 격리**:
   - 등하교(`commute`), 평일 방과후(`afterSchool`), 토요일(`saturday`) 각 노선의 배정 제외 상태는 단일 boolean 플래그가 아닌 노선 타입별 독립 필드(`excludeFromAssignmentByType`)로 분리 저장 및 조회한다.
   - 특정 노선(예: 방과후)에서 배정 제외된 버스가 다른 노선(예: 등하교)의 배정 대상이나 검색 목록에서 누락되는 간섭을 원천 차단한다.

4. **방과후 수강 명단과 스쿨버스 명단 간 동명이인 오매칭 방지**:
   - 방과후 수강생(`enrollments`)과 스쿨버스 학생(`students`) 간 상호 대조 및 자동 배정 시, 이름 단독 fallback 매칭을 절대 금지한다.
   - 반드시 고유 `studentId` 우선 매칭을 수행하고, 식별자가 없는 경우 `이름 + 학년 + 반`(동일 학년/반 엄격 일치) 복합 키로만 매칭하여 타 학년/반 동명이인으로의 오배정을 원천 차단한다.

5. **배정 대상 버스 목록의 실운행 필터링 필수**:
   - 교사 및 학생 배정 테이블/선택 모달에서는 전체 버스를 무조건 나열하지 않고, 선택된 노선/요일에 실제로 정류장(`stops`)과 운행 계획이 존재하는 실운행 버스(`isBusOperational`)만 필터링하여 노출한다.

6. **모달/팝업 스크롤 바운스 방지 (`overscroll-contain`)**:
   - 교사/학생 선택 모달 등 내부 스크롤 목록 컨테이너에는 반드시 `overscroll-contain` 스타일을 적용하여, 끝단 스크롤 시 부모 윈도우 스크롤 연쇄로 인한 화면 떨림(Jittering)을 원천 차단한다.

7. **수강 취소(`CANCELLED`) 및 비확정 신청 배제와 학기/방학 모드 격리**:
   - 스쿨버스 시스템에서 방과후 수강 명단(`afterschool_enrollments`)을 학생 데이터에 연동하거나 학생 카드에 렌더링할 때, 취소된 신청(`status === 'CANCELLED'`)이나 미확정 상태는 반드시 원천 배제하고 오직 확정(`ENROLLED`) 상태만 매핑한다.
   - 학기중 모드(`regular`)와 방학 모드(`vacation`)는 강좌의 실제 학기 속성(`semesterMode`, `semester`, `period`, `title`)에 따라 완벽히 분리 처리하며, 학기중 모드에서는 방학 강좌 수강 내역이 학생 카드나 목적지에 절대 혼선 및 간섭되지 않도록 격리한다.

8. **스쿨버스 학생 카드 내 방과후 수업 제외(X) 시 양방향 완결 동기화**:
   - 스쿨버스 관리자에서 학생의 방과후 수업 명단 제외(X)를 실행할 경우, `afterSchoolClassIds`뿐만 아니라 학생 객체의 `enrolledCourseTitles`, `afterSchoolCourseTitles`, `afterSchoolCourseTitle` 및 UI 로컬 state를 즉시 갱신해야 한다.
   - 동시에 메인 방과후 수강 컬렉션(`afterschool_enrollments`)의 해당 신청 문서도 `status: 'CANCELLED'`로 동기화하여 실시간 리스너에 의한 재등장이나 방과후 관리자 페이지와의 불일치를 원천 차단한다.

9. **학생 카드 조회 시 탑승 버스 좌석표 연동 및 미배정 조건부 렌더링**:
   - 탑승 학생 관리에서 미배정 목록 클릭 또는 전체 학생 검색으로 특정 학생을 조회할 때, 해당 학생이 현재 선택된 요일/경로의 버스 노선에 배정되어 있다면 학생 카드 내에 탑승 버스명, 좌석 번호 뱃지 및 인라인 버스 좌석표(`BusSeatMap`)를 함께 렌더링하고 해당 좌석을 시각적으로 강조한다.
   - 학생이 해당 요일/경로에 미배정 상태인 경우에는 좌석표를 렌더링하지 않고 학생 기본 카드만 간결하게 표시한다.
   - 상단 목적지 오류(배정 불가) 카드는 화면 공간을 과도하게 점유하지 않도록 기본 접힘(`isUnassignableFolded = true`) 상태로 제공한다.

10. **방과후 강좌 강사진 상호 보완 원칙 (주강사/보조/추가강사 1~4 누락 방지)**:
    - 스쿨버스 관리자의 방과후 하교 버스 조회(`AfterSchoolManagementTab`) 및 교사용 방과후 명단 조회(`AfterSchoolInquiryDialog`)에서 강좌 정보를 표출할 때, 스쿨버스 DB(`after_school_classes`) 단독 데이터에 의존하지 않는다.
    - 메인 방과후 DB(`afterschool_courses`)의 강좌 정보를 실시간 구독하여 두 데이터 소스를 상호 보완(`getComplementaryInstructors`)하며, 주강사뿐만 아니라 추가강사(1~4) 및 보조강사(`assistantTeachers`, `assistantInstructor`) 전체를 식별하여 수업 선택 드롭다운, 검색 필터, 선택된 강좌 상세 카드 상단에 명확한 뱃지 및 전체 강사 명단으로 표출한다.

11. **탑승 명단 및 학생 카드 내 방과후 강좌 요일별 엄격 일치 (타 요일 강좌 노출 금지)**:
    - 스쿨버스 교사용 페이지(`/teacher/bus`) 등 버스 탑승 학생 명단 및 학생 카드에서 방과후 강좌명/뱃지를 표출할 때, 학생이 수강하는 전체 요일 강좌명(`afterSchoolCourseTitle`, `enrolledCourseTitles`)을 무조건적인 fallback으로 노출하지 않는다.
    - 반드시 현재 선택된 요일(`selectedDay`)에 실제로 수업이 진행되는 강좌(`afterSchoolCoursesByDay[selectedDay]`)만 대조하여 표출하고, 해당 요일에 방과후 수업이 없는 경우에는 뱃지 및 상세 카드의 방과후 항목을 일절 노출하지 않아 일반 하교 버스 탑승 시의 혼선을 원천 차단한다.

12. **로그인 교사 맞춤형 기본 선택 우선순위 (담당 버스 및 본인 방과후 강좌 우선 선택)**:
    - 스쿨버스 탑승 관리(`/teacher/bus`) 진입 시, 비동기 데이터 로딩 중 성급하게 '전체(`all`)'로 초기화하지 않으며, 로그인한 교사의 담당 버스(등하교/방과후/토요)가 확인되는 즉시 해당 버스를 1순위로 자동 선택한다. 담당 버스가 아예 없는 교사인 경우에만 전체(`all`)를 표시한다.
    - 방과후 수업 관리(`/teacher/afterschool`) 진입 시, 관리자 권한(`isAdmin`)이 있더라도 가나다 순 첫 번째 강좌가 아닌 로그인한 교사의 본인 담당 강좌(`myOwnCourses`: 주강사/보조/추가강사) 출석부를 최우선으로 기본 선택하고, 드롭다운 목록에서도 본인 강좌를 최상단에 배치하여 대리 출석 관리 편의성과 본인 수업 출석 확인의 직관성을 보장한다.

13. **하교 미배정 명단 내 방과후 이동 학생 식별 배지 ('종료후복귀') 표준화**:
    - 방과후 수업을 수강하여 방과후 버스 노선으로 이동한 학생은 방과후 종료 후 원래 하교 버스로 복귀해야 하므로 정규 하교 버스 좌석에서는 배제되어 하교 미배정 명단에 머무르게 된다.
    - 관리자가 하교 버스 좌석 배정 작업 중 이 학생들을 미배정으로 오인하여 하교 버스에 중복/재배정하는 혼선을 원천 차단하기 위해, 하교(`routeType: 'Afternoon'`) 미배정 카드에서는 해당 요일에 방과후 수업/목적지가 있는 학생에게 방과후 강좌명 배지 옆에 `'종료후복귀'`(`amber` 톤 배지)를 필수 노출한다.
    - 관리자 버스 페이지(`/admin/bus`)에서도 학생 데이터 병합 시 요일별 방과후 강좌(`afterSchoolCoursesByDay`)를 구축하여 타 요일 강좌 간섭 없이 해당 요일의 방과후 정보와 복귀 배지가 정확히 매핑되도록 보장한다.

14. **학생 카드 팝업 레이아웃 및 요일별 배정 노선 컴팩트화**:
    - 스쿨버스 학생 상세 카드 Dialog(`StudentGlobalSearchPanel`)는 모달 내 불필요한 세로 스크롤을 방지하기 위해 폼 요소를 행 단위 2분할(`grid-cols-2`: 성별/전화번호, 등하교 목적지) 및 3분할(`grid-cols-3`: 학년/반/번호)로 컴팩트하게 배치한다.
    - 이름 편집 및 개인 QR 인쇄 등 액션 버튼은 이름 타이틀 우측의 빈 헤더 공간(`DialogHeader`)에 인라인으로 배치한다.
    - 배정된 노선 목록은 개별 카드 수직 나열 대신 요일별 단일 행(`weeklyAssignedRoutes`: 요일 | 등교 | 하교 | 방과후) 그리드 테이블로 통합 렌더링하고, 각 셀 내에 버스 번호, 목적지 축약 뱃지 및 원터치 해제(`UserX`) 버튼을 함께 제공한다.
    - 모든 폼 라벨, 플레이스홀더 및 액션 버튼은 하드코딩된 원본 키 문자열 노출을 원천 차단하고 `t()` 다국어 사전(`ko`, `en`, `vi`)과 기본 fallback 파라미터를 반드시 함께 적용한다.

15. **통합 학생 관리 중심 형제·자매 연결 및 스쿨버스 실시간 양방향 동기화**:
    - 형제자매 연결/해제 관리는 스쿨버스 시스템이 아닌 '통합 학생 관리'(`/admin/students`) 프로필에서 전담한다.
    - 마스터 학생 데이터의 `siblingGroupId` 변경 시 `masterStudentService`를 통해 메인 DB(`master_students`)와 스쿨버스 DB(`kisbusDb.students`)에 원자적(`writeBatch`)으로 동시 반영하며, 스쿨버스 시스템은 `onMasterStudentsUpdate` 리스너를 통해 형제자매 그룹 정보를 실시간으로 구독하여 학생 카드 및 배정 현황에 즉시 매핑한다.

16. **스쿨버스 하차 목적지 단일화와 예비 목적지 관리 및 직관적 좌석 배정 모달**:
    - 학생의 하차 목적지는 평일 하교 목적지(`afternoonDestinationId`)로 기본 통일하고, 방과후 및 토요일 하차 목적지도 이를 기본 참조하도록 한다.
    - 특정 요일에 다른 목적지에서 하차하는 경우에 한해 `[예비 목적지 추가]` 버튼을 통해 요일별 목적지를 등록/삭제하며, 미등록 시에는 추가 버튼만 노출하여 UI를 간결하게 유지한다.
    - 학생 카드에서 '좌석 배정' 버튼을 누르면 직관적인 전용 모달 팝업(`AssignSeatDialog`)이 열려 요일, 경로, 버스를 선택하고, `BusSeatMap` 좌석표에서 빈자리를 원터치 선택하여 명시적으로 배정할 수 있도록 제공한다.

17. **교사용 스쿨버스 학생 카드 팝업화(Dialog) 및 3대 핵심 액션 버튼 1열 배치**:
    - 교사용 스쿨버스 페이지(`/teacher/bus`)에서 학생을 검색하거나 전체 버스 목록(`AllStudentsBoardingStatus`) 및 개별 버스 탑승 명단에서 학생을 클릭했을 때, 기존의 페이지 스크롤 이동 인라인 카드 대신 전용 `Dialog` 팝업 모달을 띄운다.
    - 팝업 카드에는 학생의 학년, 반, 번호(있는 경우), 이름, 연락처(전화걸기 링크), 해당 요일/경로 목적지, **오늘 버스 번호 뱃지**(`studentBusNo`, 미배정 시 '미배정'), **오늘 방과후 수강 강좌**(방과후 경로 시 필수)를 명확히 표출한다.
    - 하단 액션 바는 `grid grid-cols-3 gap-2 w-full` 그리드를 적용하여 **[오늘 안 탐 / 탑승 복구]**, **[요일 제외]**, **[조장 임명 / 조장 해제]** 3개 버튼을 반드시 나란히 한 줄로 배치한다. 전체 버스 보기(`selectedBusId === 'all'`) 상태에서도 `allRoutes` 및 `allStaticRoutes`를 참조하여 해당 학생의 배정 노선을 찾아 세 가지 액션이 모두 정상 작동하도록 보장한다.

18. **스쿨버스 좌석 배치표 A4 단일 페이지 인쇄 표준 (독립 팝업 창)**:
    - 교사용 스쿨버스 페이지(`/teacher/bus`) 등에서 버스 좌석 배치표를 인쇄할 때, 전체 페이지 `window.print()`를 직접 호출하지 않는다. 전체 레이아웃의 헤더, 탑승 명단, 분실물 카드 및 `@media print` 스타일 간섭으로 인해 상단에 다른 UI가 겹치거나 좌석표가 찌그러지는 현상이 발생하기 때문이다.
    - 반드시 `window.open('', '_blank')` 독립 팝업 창을 열어 버스 정원(16인승, 29인승, 45인승)에 맞춘 단일 격자 그리드 HTML 문서를 생성하고, 해당 창에서 `window.print()`를 자동 트리거한다.
    - 인쇄 양식에는 버스명, 운행 일자, 요일/경로 뱃지, 좌석별 배정 학생명(조장 👑 뱃지, 오늘 안 탐 뱃지 포함), 운전석, 레이아웃 외 임시 배정 학생 목록(Overflow) 및 총 탑승 인원을 포함하여 단 1장의 A4에 깔끔하게 인쇄되도록 보장한다.

19. **교사용 탑승 명단 상태 변경 버튼과 학생 상세 카드 상호작용 분리**:
    - 전체 버스 탑승 현황(`AllStudentsBoardingStatus`) 및 개별 버스 탑승 명단에서 학생의 상태(미탑승/탑승/하차 완료/오늘 안 탐) 버튼을 클릭/터치할 때, 상위 행(`TableRow`)의 학생 카드 열림 이벤트가 트리거되지 않도록 상태 셀(`TableCell`) 및 버튼에 이벤트 전파 방지(`e.stopPropagation()`, `e.preventDefault()`)를 필수로 적용한다.
    - 상태 버튼 클릭 시 `미탑승` -> `탑승` -> `하차 완료` -> `미탑승` (오늘 안 탐 상태 시 `탑승`) 순으로 즉시 순환 토글되며, Firestore 출석 데이터(`updateAttendance`) 갱신을 통해 실시간으로 좌석표(`BusSeatMap`)와 상태 배지에 양방향 동기화되어야 한다.
    - 학생 상세 카드 Dialog(`onSelectStudent`)는 버튼 밖의 학생 이름이나 버스 정보 셀을 탭했을 때만 안정적으로 열리도록 명확히 분리한다.
    - 다국어 사전(`ko`, `en`, `vi`)에서 `teacher_page.status_notRiding` 및 `teacher_page.status_not_riding_today` 번역 키를 누락 없이 상호 보완하여 화면에 원본 키 문자열이 노출되는 것을 원천 차단한다.

20. **교사용 전체 버스 현황 모바일 퀵 네비게이션 및 컴팩트 헤더 표준**:
    - 모바일 화면에서는 전체 버스 탑승 명단(`AllStudentsBoardingStatus`)과 조장 현황(`AllGroupLeadersStatus`)이 세로로 길게 배치되므로, 헤더 액션 바에 직관적인 원터치 앵커 이동 버튼을 제공한다.
    - 탑승자 명단 헤더: 미탑승자 복사 버튼을 모바일에서 아이콘 전용(`hidden sm:inline`)으로 컴팩트화하고, 바로 옆에 왕관 아이콘 버튼(`Crown`, `lg:hidden`)을 배치하여 조장 현황 섹션(`all-group-leaders-section`)으로 즉시 부드럽게 스크롤(`scrollIntoView`) 이동한다.
    - 조장 현황 헤더: 모바일에서는 타이틀을 `'조장 현황'`으로 축약 표출하고, 타이틀 바로 옆에 윗쪽 화살표 버튼(`ArrowUp`, `lg:hidden`)을 배치하여 탑승자 명단 상단(`all-students-boarding-section`)으로 즉시 복귀할 수 있도록 보장한다.
    - 각 타겟 컨테이너에는 `scroll-mt-20`을 명시하여 고정 상단 헤더에 콘텐츠 상단이 가려지는 현상을 원천 차단한다.

21. **모바일 방과후 출석부 1터치 순환 토글 및 학생 카드 상호작용 분리 표준**:
    - 방과후 교사용 출석부(`/teacher/afterschool`) 모바일 화면의 학생별 출결 체크는 팝업 드롭다운 방식 대신 원터치 즉시 순환 토글(`미체크(·)` -> `출석(○)` -> `지각/개별(△)` -> `결석(×)` -> `미체크(·)`) 방식을 기본 표준으로 적용한다.
    - 출결 버튼 영역 터치 시 `e.stopPropagation()` 및 `e.preventDefault()`를 필수 적용하여 좌측 학생 정보/카드 열림(`setModalStudent`) 이벤트와의 충돌을 원천 차단한다.
    - 주 1회 등 복수 요일 강좌에서 특정 요일 미수강생(`mark === '―'`)은 비활성화 상태를 유지하여 오입력을 방지하고, 출결 변경 시 Firestore 출석 레코드와 해당 요일 스쿨버스 탑승 여부(`syncBusAbsenceForDay`)에 즉시 실시간 동기화한다.

## 학부모 신청서 및 공문서 출력/결재 서식 규칙
1. **결재란 직책 슬롯 표준화**:
   - 상단 결재란의 헤더 직책명은 결재자의 세부 보직명(`교무부장`, `학년부장`, `1반 담임` 등)을 직접 노출하지 않고, 반드시 표준 슬롯명(`['담임', '부장', '교감', '교장']`) 2글자 고정으로 렌더링하여 결재란 너비 붕괴 및 텍스트 쏠림을 방지한다.

2. **원본대조필 직인 노출 격리 (학부모 사본 전용)**:
   - 교원/관리자 결재문서함(`/documents/[id]`)은 학교 보관용 원본 문서이므로 하단 여백의 '원본대조필' 직인을 절대 표시하지 않는다.
   - 원본대조필 직인은 오직 학부모 포털(`/parents/documents/[id]`, 인쇄 및 PDF 다운로드)에서 결재가 완료(`approved`)된 문서에 한해 하단 우측 여백에 날인한다.

3. **A4 1페이지 규격 및 인라인 테두리 보존**:
   - 공문서 및 신청서 양식은 브라우저 인쇄와 PDF 변환 시 테두리가 유실되지 않도록 모든 `table`, `th`, `td`에 인라인 스타일(`border: '1px solid #000000'`, `borderCollapse: 'collapse'`)을 명시한다.
   - 1페이지 독립 서식(서식 1 신청서, 서식 2 결과보고서, 서식 3 결석계)은 A4 규격(297mm) 내에 완벽히 수납되도록 행 높이와 여백을 정밀 계산하여 하단 서약란 및 직인이 다음 페이지로 밀리는 것을 원천 차단한다.

4. **PDF 변환 엔진 및 화면 1:1 캡처 보존**:
   - A4 서식의 PDF 다운로드 변환 시 가상 뷰포트 재렌더링으로 왜곡을 유발하는 `html2canvas` 대신, 브라우저의 실제 렌더링 픽셀을 그대로 캡처하는 `html-to-image`(`toPng`, `pixelRatio: 2.5`, `cacheBust: true`, `width: el.offsetWidth, height: el.offsetHeight`)를 사용한다.
   - 사용자가 화면에서 보고 있는 렌더링 결과와 1픽셀의 오차도 없이 1:1로 일치하도록 보장한다.

5. **서명란 및 인명/직함 라벨 줄바꿈 원천 차단 (`whitespace-nowrap`)**:
   - 서약 및 서명 영역의 '보호자 :', '학부모 :', '학생 :' 등 라벨과 기안자/보호자명, 직인 슬롯은 폭 축소 시 콜론(`:`)이나 텍스트가 다음 줄로 떨어지지 않도록 반드시 `whitespace-nowrap` 및 인라인 플렉스 컨테이너(`inline-flex items-center whitespace-nowrap`)로 묶어 렌더링한다.

6. **테이블 셀 수직 중앙 정렬 및 2mm 여백 표준화**:
   - 모든 단일행 테이블 셀(`th`, `td`)은 텍스트가 셀 하단에 붙지 않도록 `display: flex; align-items: center;` 컨테이너 및 `padding: '2mm'`(또는 내부 `min-h` 및 `leading-tight`)를 적용하여 완벽한 수직 중앙 정렬과 시각적 여백을 유지한다.

## 통합 학생 마스터 및 대용량 데이터 로딩 규칙
1. **온디맨드 그룹(학년) 쿼리 필수 (초기 대량 패치 원천 차단)**:
   - 통합 학생 계정 관리(`/admin/students`) 등 수천 건 이상의 문서를 다루는 대용량 테이블에서는 초기 진입 시 전교생 컬렉션을 일괄 구독(`onSnapshot`)하지 않는다.
   - 반드시 사용자가 조회할 학년(`selectedGrade`)을 선택했을 때 해당 그룹에 한정하여 `where('grade', 'in', gradeValues)` 조건부 쿼리를 실행하며, 초기 상태는 '선택 안 함' 상태로 두고 직관적인 빈 상태 안내 UI와 바로가기 버튼을 제공하여 초기 로딩 지연과 불필요한 네트워크 트래픽을 원천 차단한다.

## 데스크톱 반응형 뷰포트 및 고정 푸터 레이아웃 규칙
1. **넓은 화면(데스크톱) 하단 푸터 고정 및 모바일 스크롤 격리**:
   - `AppLayout`에서 데스크톱(`lg:`) 환경은 최하단 푸터(`AppFooter`)를 `shrink-0`으로 화면 바닥에 고정하고, 본문 컨테이너는 `flex-1 min-h-0 overflow-y-auto`로 구성하여 푸터 위 남은 뷰포트 공간을 100% 수납한다.
   - 모바일 환경은 기존대로 `main` 컨테이너의 `overflow-y-auto pb-20`을 유지하여 자연스러운 전체 세로 스크롤 끝에 푸터가 노출되도록 격리한다.

2. **대시보드 비율형 뷰포트 맞춤 (Zero-Scroll 유지 및 고정 min-h 배제)**:
   - 전자결재 대시보드(`/inbox`) 등 한눈에 전체 현황을 파악해야 하는 메인 대시보드 화면에서는 카드 내부 컨테이너에 고정된 픽셀 높이(`min-h-[220px]` 등)를 강제하지 않는다.
   - 모든 수직 패널은 `flex-1 min-h-0`과 `overflow-y-auto scrollbar-thin`을 적용하여 화면 크기(노트북 768p부터 고해상도 모니터까지)에 비례하여 남은 공간을 유연하게 채우고, 내부 항목이 넘칠 때만 해당 카드 안에서 스크롤되도록 하여 브라우저 전체 창에 불필요한 스크롤바가 발생하는 것을 원천 차단한다.

3. **15.6인치 디스플레이 및 PWA Standalone 헤더 요소 겹침 방지 (Flexbox 격리 및 안전 축약)**:
   - 최상단 전역 헤더(`AppHeader`)는 `fixed top-0`에 의존하여 레이아웃 컨테이너 외부 패딩(`pt-16`)과 충돌을 유발하지 않고, 플렉스 루트 내 `shrink-0` 요소로 자연스럽게 배치한다.
   - 좌측 영역(로고 및 사이드바 토글 버튼)은 반드시 `shrink-0` 및 `whitespace-nowrap`을 적용하여 우측 요소에 의해 압축되거나 찌그러지지 않도록 방어한다.
   - 우측 사용자 정보(소속/담당/직책)는 15.6인치 노트북(125%~150% 배율 시 실제 가로폭 약 1280px~1536px) 및 브라우저 컨트롤 바가 제거된 PWA Standalone 모드에서 가로폭 부족으로 좌측 로고 위로 겹쳐지는 현상을 원천 방지하기 위해 `hidden xl:flex` 및 `min-w-0 truncate`를 필수로 적용한다.

## 학교 조직도 및 전자결재 수기 결재선 연동 규칙
1. **학교 리더십 수기 결재자(학교장/행정실장 등) 성명 직접 입력 및 공문서 결재선 자동 바인딩**:
   - 학교장(교장), 행정실장 등 전산 계정이 없어 수기 결재를 진행해야 하는 직책의 경우, 시스템 설정(조직도 > 학교 리더십)에서 계정 매핑이 없더라도 성명을 직접 입력(`principalName`, `administrativeHeadName` 등)하여 저장할 수 있도록 지원한다.
   - 공문서 작성기(`document-form`) 및 복무/초과근무 신청기(`teacher/duty`, `teacher/overtime`)는 조직도 설정에 성명이 등록되어 있는 경우 가상 프로필을 지원하고 결재선 생성 시 해당 성명을 `approver.name`으로 즉시 바인딩하여 결재란에 수기 결재자의 성명이 누락 없이 정확하게 인쇄/표출되도록 보장한다.
   - 3번째 결재 슬롯은 '협조' 외에 '행정실장'을 직책으로 선택할 수 있도록 지원하고, 선택 시 행정실장의 등록 성명이 결재선에 즉각 동기화되도록 한다.

## 반복 루틴 업무 자동화 및 부장 그룹 일괄 할당 규칙
1. **루틴(반복) 업무 주기 자동 산출 및 대시보드 리마인드 동기화**:
   - 주간/월간 단위로 정기 반복되는 업무(안전 점검표, 감염병 현황, 부장회의록 등)는 '루틴 업무 요청(`routine`)' 카테고리로 등록하며, `routineConfig`를 통해 반복 주기(`weekly`/`monthly`), 기준 요일/일자, 소요기간을 저장한다.
   - 전자결재 대시보드(`/inbox`)는 현재 기준일이 해당 루틴 업무의 활성 주기에 해당하고 본인이 미제출 상태인 경우, '나의 업무' 카드 상단에 루틴 알림 배너를 즉각 표출하여 주초/월초에 잊지 않고 신속하게 제출할 수 있도록 리마인드한다.
   - 업무 목록 및 제출 현황 모달 등 모든 뷰에서는 일반 업무와 구별되도록 '루틴 (주간/월간)' 배지를 필수로 노출한다.

2. **부장 그룹(`head`) 대상 자동 통합 추출**:
   - 부장 회의록, 부장단 공지 등 학교 리더십/중간관리자 전체를 대상으로 하는 업무 요청 시 '부장 그룹' 버튼을 제공한다.
   - 부장 그룹 선택 시 별도의 개별 선택 없이도 학교 조직도(`orgData`) 상의 행정 부서 부장(`departments.headEmail`), 각 학년 부장(`gradeHeads`), 교무부장(`academicHead`)을 자동으로 중복 없이 단일 대상군으로 추출하여 1회 클릭으로 정확하게 일괄 할당한다.

## 방과후학교 출석부 모바일 사용성 및 레이아웃 규칙
1. **모바일 뷰포트 본문 세로 스크롤 허용 (`overflow-y-auto`)**:
   - `MainLayout`의 `<main>` 컨테이너는 데스크톱 고정 뷰포트 최적화 시에도 모바일 화면에서 자식 요소가 길어졌을 때 아래로 자연스럽게 스크롤될 수 있도록 반드시 `overflow-y-auto overflow-x-hidden`을 유지한다. `overflow-hidden`으로 닫아 수강생 명단이나 긴 서식이 잘리는 현상을 원천 차단한다.

2. **모바일 출결 체크 원터치 순환 및 명시적 액션 바 보존**:
   - 모바일 학생 출석부 카드에서는 터치 편의성을 극대화하기 위해 출결 마크 버튼을 탭했을 때 `미체크(·) -> 출석(○) -> 지각/개별(△) -> 결석(×)` 순으로 즉각 1터치 순환 토글되도록 지원하고, 세부 드롭다운 메뉴도 함께 제공한다.
   - 모바일 회차 바 상단에는 '전원출석', '공유', '인쇄' 3대 핵심 제어 버튼을 아이콘 단독이 아닌 텍스트 라벨과 함께 눈에 띄게 배치하여 기능 접근성을 보장한다.

3. **모바일 목록 하단 드롭다운 방향 제어 (위쪽 표출 `dropUp`)**:
   - 모바일 출석부 등 긴 목록의 하단 항목(마지막 2개 이상 및 뷰포트 하단 공간 220px 미만)에서는 출결 메뉴나 드롭다운 팝업이 화면 아래(네비게이션 바 뒤)로 잘려 나가지 않도록 반드시 `bottom-full mb-1` 방향으로 위로 열리도록 제어한다.

4. **교원 및 관리자 전용 페이지의 `MainLayout` 표준 적용 (하단 네비 및 뒤로가기/홈 보장)**:
   - 학교 체육(`/teacher/pe`), 보건실, 방과후, 스쿨버스 등 교원 페이지는 반드시 공통 레이아웃인 `MainLayout`으로 감싸 렌더링한다.
   - 모바일 뷰에서 상단 헤더의 뒤로가기(`ArrowLeft`), 결재 홈(`Home`) 바로가기 및 최하단 5대 고정 네비게이션 바(`MobileBottomNav`: 방과후, 스쿨버스, 새문서, 학교체육, 학생계정)가 유실 없이 일관되게 제공되도록 보장한다.

## 담임 교사 업무 관리소(출석부) 모바일 뷰포트 고정 및 컴팩트 5대 탭 표준
1. **모바일 뷰포트 고정(`isFixedScreen`) 및 명단 독립 스크롤**:
   - 담임 업무(`/teacher/homeroom`) 등 상단 헤더, 5대 탭, 학급/날짜 선택 헤더가 화면에 상시 고정되어야 하는 모바일 페이지는 `MainLayout`에 `isFixedScreen={true}`를 적용하여 `h-dvh max-h-dvh overflow-hidden`으로 부모 윈도우 스크롤을 원천 차단한다.
   - 상단의 앱바, 페이지 타이틀, 5대 탭, 카드 헤더는 `shrink-0`으로 상단에 고정 배치하고, 오직 내부 학생 명단 목록 컨테이너만 `flex-1 min-h-0 overflow-y-auto overscroll-contain`으로 부드럽게 스크롤되도록 격리한다.

2. **5대 핵심 탭 아이콘화 1줄 배치 및 장황한 설명글 배제**:
   - 모바일 화면에서는 탭이 2줄 이상으로 꺾이거나 아래 카드와 겹치지 않도록 `grid-cols-5` 단일 행으로 구성하고, 아이콘과 2~3글자 핵심어(출석부, 대리작성, 학급관리, 학년자료, 학부모상담)로 컴팩트하게 축약한다.
   - 상단 헤더 및 카드 내의 장황한 부가 설명글은 과감히 제거하거나 모바일에서 숨겨 5mm(약 `p-2`) 수준의 컴팩트한 상하 여백과 시원한 명단 뷰포트를 확보한다.

3. **모바일 원터치 출결 순환 토글 및 연락처 인라인 액션**:
   - 모바일 학생 출석부 리스트에서는 폭이 좁은 화면에서 가로 8개 컬럼 테이블을 스크롤하지 않고, 학생 이름 및 보호자 연락처(전화걸기 링크), 버스 뱃지와 함께 출결 상태 버튼을 원터치하여 `출석` -> `결석` -> `조퇴` -> `개별하교` 순으로 즉각 순환 토글되도록 제공하여 모바일 입력 편의성을 극대화한다.

4. **담임 대리작성 모바일 1화면 수납(Zero-Scroll) 및 TabsContent 비활성 패널 격리 표준**:
   - Radix UI `TabsContent`에 `flex` 속성을 부여할 때는 반드시 `data-[state=active]:flex data-[state=active]:flex-1 data-[state=inactive]:hidden` 패턴을 엄격히 적용한다. 무조건적인 `flex` 클래스는 Radix의 기본 숨김 속성을 무효화하여 비활성 탭이 화면 공간을 계속 점유하고 활성 탭 아래에 거대한 빈 공간(오탐 여백)을 유발하기 때문이다.
   - `MainLayout`의 `isFixedScreen` 모드 사용 시, 모바일 하단 네비게이션 바(`h-16`, 64px)에 의해 하단 액션 버튼이나 콘텐츠가 가려지는 현상을 방지하기 위해 `pb-16 lg:pb-0` 클래스는 반드시 `contentClassName` 뒤에 배치하여 패딩 축약(shorthand `p-*`)에 의해 덮어씌워지지 않도록 단단히 보호한다.
   - 대리작성 서식(교외체험학습 신청서 및 결석계)은 신청 기간 및 일수를 인라인 뱃지 헤더로 통합하고 폼 카드를 `flex flex-col justify-between` 구조로 컴팩트화하여, 390x844 모바일 뷰포트에서 상하단 공간 낭비 없이 꽉 차고 스크롤바가 전혀 생기지 않는 1화면 수납(Zero-Scroll)을 보장한다.

## 방과후 버스 번호 요일별 독립 조회 규칙

1. **방과후 출석부/수강생 명단 버스 번호 요일별 실시간 매핑**:
   - 방과후 출석부(`AttendanceManagement.tsx`), 외부 강사 공유 출석부(`attendance/share/[courseId]/page.tsx`), 수강생 명단(`StudentManagement.tsx`) 등 방과후 버스 번호를 표출하는 모든 곳에서 학생 문서 단일 필드(`afterSchoolBusNo`)에 의존하지 않는다.
   - 반드시 `kisbus` DB의 `routes` 컬렉션을 실시간 구독(`onRoutesUpdate`)하고, `type === 'AfterSchool'` 노선에서 해당 학생(`studentId`)이 배정된 좌석(`seating`)을 찾아 `dayOfWeek`와 `busId`를 조합하여 `busesByDay: Record<string, string>` 요일별 버스 맵을 구성한다.
   - 출석부에서 특정 회차(날짜)를 선택했을 때 `activeDay.dateStr`에서 한글 요일(`(월)`, `(수)` 등)을 파싱하거나 `fullDate`에서 JS Date 요일 인덱스를 추출하여 영문 요일(`Monday`, `Wednesday`)로 변환한 뒤 `busesByDay[targetDayEn]`을 1순위로 조회한다.
   - 주 2회 이상 서로 다른 버스를 타는 학생(예: 월요일 39A호, 수요일 37호)은 수강생 명단에서 요일별 뱃지(`월: 39A호차`, `수: 37호차`)로 각각 표출하고, 출석부에서는 현재 선택된 회차 요일 버스만 단독 표출하여 혼선을 원천 차단한다.

2. **외부 강사 공유 출석부의 kisbus routes/buses 실시간 구독 필수**:
   - `attendance/share/[courseId]/page.tsx`는 외부 강사가 접근하는 공유 페이지이지만, 방과후 버스 번호 정확성을 위해 `onRoutesUpdate` 및 `onBusesUpdate` 구독을 포함해야 한다.
   - `getStudentBusNo` 호출 시 현재 선택된 `activeDay`를 6번째 인자(`targetDay`)로 전달하여 요일별 버스를 정확히 조회한다.

## 빌드 전략 및 TypeScript 오류 관리

1. **App Hosting 환경에서의 배포 전략**:
   - `firebase.json`에 `alwaysDeployFromSource: true`가 설정된 App Hosting 프로젝트에서는 `npm run build` 대신 `npm run typecheck`(tsc --noEmit)로 타입 오류만 전수 검사한 뒤 `git push`로 배포를 트리거한다.
   - Windows 환경에서 dev 서버가 실행 중일 때 `npm run build`를 실행하면 `.next\trace` 파일 락(EPERM) 또는 응답 없음이 발생할 수 있으므로, App Hosting 환경에서는 로컬 빌드 시도를 생략하고 typecheck → git push 배포 파이프라인을 표준으로 사용한다.

2. **TypeScript 기존 오류 우선 수정 원칙**:
   - 새 기능 추가 시 기존 파일에 이미 존재하는 TS 오류(타입 불일치, 미사용 프로퍼티 등)를 발견하면 함께 수정한다.
   - 특히 `modalStudent` 등 상태 타입이 컴포넌트 내부에서 확장되는 경우, 타입 선언에 선택적 필드(`busesByDay?: Record<string, string>`)를 추가하여 타입 안전성을 보장한다.

## 실무 전담 계정(직책: '담당')의 진입 경로 격리 및 리다이렉트 보장 규칙
1. **전담 계정 로그인 리다이렉트 및 관리자 지정 연동**:
   - 전자결재나 일반 교원 기능을 사용하지 않고 특정 현장 업무(스쿨버스 등)만 전담하는 계정은 `UserProfile.loginRedirectUrl` 필드를 통해 로그인 직후 전용 페이지로 즉시 자동 이동시킨다.
   - 시스템 설정의 교직원 관리 탭에서 직책이 '담당'인 계정을 추가하거나 편집할 때, 로그인 후 이동할 목적지 페이지(`/admin/bus`, `/teacher/bus` 등)를 지정할 수 있는 UI를 필수로 제공한다.
   - `bus@kshcm.net` 등 표준 시스템 전담 계정은 DB 상에 명시적 URL 필드가 누락되어 있더라도 로그인 시 기본 목적지(`/admin/bus`)로 자동 보정 및 저장하여 사용자 조작 누락에 따른 혼선을 원천 방지한다.
2. **직책 '담당'의 사이드바 원천 비활성화 및 접힘 상태 디폴트 고정**:
   - 직책이 '담당'(`profile?.role === '담당'`)인 사용자는 전자결재 사이드바를 전혀 사용하지 않으므로, `SidebarProvider`에서 로컬스토리지 저장 여부와 무관하게 `isSidebarOpen: false`를 디폴트로 고정하고 토글 기능을 차단한다.
   - 상단 `AppHeader`에서도 담당 직책 사용자에게는 사이드바 열기/접기 버튼을 렌더링하지 않아 온전한 전용 화면 공간을 확보한다.

## 스쿨버스 관리자 레이아웃 스크롤 컨테이너 및 헤더 Sticky 고정 규칙
1. **내부 스크롤 격리 및 탭/필터 헤더 무이탈 고정 (`sticky top-0`)**:
   - 스쿨버스 관리자(`/admin/bus`) 등 대량의 학생 카드와 좌석표를 스크롤하는 대형 관리자 화면은 `MainLayout`에 `contentClassName="p-0 h-full overflow-y-auto"`를 부여하여 상단 스쿨버스 헤더 아래 본문 영역이 독립 스크롤 컨테이너가 되도록 격리한다.
   - 내부의 버스 탭 및 요일/경로/정류장 필터 래퍼는 `sticky top-0`으로 설정하여, 학생 카드나 미배정 목록을 아래로 길게 스크롤하더라도 상단 헤더 및 필터 바가 뷰포트 밖으로 밀려 올라가거나 해제되지 않고 상단에 완벽히 고정되도록 보장한다.

## 대시보드 주요 업무 바로가기 교직원 역할별 격리 및 동적 프리셋 규칙
1. **역할/담당 기반 선택 목록 엄격 격리**:
   - 대시보드(`/inbox`) 상단 주요 업무 바로가기 및 설정 모달(`MajorTasksModal`)에서는 모든 교사에게 전체 업무나 특정 업무를 무조건 나열하지 않는다.
   - 반드시 로그인한 교사의 실제 담당 직책/권한(담임, 방과후 강좌, 스쿨버스 탑승/관리, 체육, 보건, 복무 등)에 해당하는 업무(`availableTasks`)만 선택 후보로 제공하여, 본인의 업무가 아닌 기능이 노출되거나 선택되는 것을 원천 차단한다.
2. **계정별 로컬 저장소 격리 및 하드코딩 프리셋 배제**:
   - 주요 업무 바로가기 설정은 브라우저 공용 키가 아닌 사용자 이메일별 고유 키(`kis_dashboard_major_tasks_v2_${email}`)로 분리 저장한다.
   - 특정 업무(방과후/스쿨버스 등)를 일괄 기본값으로 강제하지 않으며, 저장된 값이 없을 때는 해당 사용자가 실제로 담당하고 있는 업무 중 상위 최대 3개를 기본값으로 자동 산출한다.

## 방과후 및 스쿨버스 '강사' 직책의 접근 권한 및 데이터 엄격 격리 규칙
1. **직책 '강사'의 접근 범위 한정 및 라우팅 격리**:
   - 교직원 직책 중 '강사'(`role === '강사'`)는 오직 **본인의 방과후 출석부(`/teacher/afterschool`)**와 **스쿨버스 탑승 관리(`/teacher/bus`)**에만 접근 권한을 갖는다.
   - 전자결재(`/inbox`, `/sent`, `/pending` 등), 교원 서비스(복무, 초과근무, 보결), 학생 계정, 학교 체육, 보건 등 타 업무 및 기안 작성(`/new`) 화면은 사이드바, 모바일 하단바, 레이아웃(`(app)/layout.tsx`, `MainLayout`) 레벨에서 원천 숨김 및 차단한다.
   - 로그인 직후 및 비인가 경로 직접 URL 입력 시 기본 목적지인 `/teacher/afterschool`로 자동 리다이렉트한다.
2. **방과후 강좌 및 출석부의 본인 담당 강좌(`myOwnCourses`) 엄격 제한**:
   - 강사 직책인 사용자가 방과후 교사 페이지(`/teacher/afterschool`)에 진입할 경우, 일반 교사와 달리 강좌 목록(`teacherCourses`)에 전체 강좌 fallback을 절대 허용하지 않고 오직 본인이 주강사/추가강사(1~4)/보조강사로 배정된 강좌(`myOwnCourses`)만 반환한다.
   - 타 강사의 강좌, 수강생 명단 및 출석부 데이터가 노출되거나 수정되는 것을 원천 차단한다.

## 시스템 관리자 권한 토글 및 전담 계정 격리 규칙
1. **관리자 권한 강제 주입(Hardcoded Admin)의 슈퍼 관리자 한정 원칙**:
   - `auth-provider.tsx` 등 전역 인증 프로필 로더에서 `isAdmin: true`를 강제 주입하는 대상은 오직 최상위 슈퍼 관리자(`beside1s@kshcm.net`)로 한정한다.
   - 현장 전담 계정(`bus@kshcm.net` 등)에 `isHardcodedAdmin = true`를 부여하여 관리자가 시스템 설정에서 관리자 토글 스위치를 꺼도 다시 켜지게 만드는 하드코딩을 원천 차단한다.
2. **관리자 토글 변경 시 세션 상태 실시간 동기화**:
   - 시스템 설정의 사용자 관리 탭에서 특정 계정의 `isAdmin` 스위치를 변경할 때, 현재 로그인한 사용자 본인의 계정이면 `updateProfile({ isAdmin: checked })`를 즉시 호출하여 헤더의 설정 톱니바퀴 아이콘 및 관리자 메뉴가 페이지 새로고침 없이 즉각 동기화되도록 보장한다.
   - 설정 모달 열림 시(`isOpen === true`) 사용자 목록(`fetchUsers(true)`)을 강제 무효화하여 최신 Firestore 값을 온전히 반영한다.

## 방과후 공식 출석부 인쇄 레이아웃 규칙

1. **30명 초과 시 2페이지 분리 원칙 (잘림 원천 차단)**:
   - `OfficialAttendanceSheet.tsx`는 `students.length > 30`(ROWS_PER_PAGE)일 때 자동으로 2페이지로 분리한다.
   - 1페이지(1~30번), 2페이지(31번~)에 동일한 제목 + 지도교사 영역을 렌더링하고, 일련번호는 연속으로 이어진다.
   - CSS: `attendance-page-block { height: 277mm; page-break-after: always }`, 마지막 블록은 `page-break-after: avoid`.
   - `overflow: hidden + max-height` 구속은 단일 페이지 전용 클래스(`attendance-page-single`)에만 적용하며, 다중 페이지 래퍼에는 `overflow: visible`을 적용한다.

2. **출석부 인쇄 시 React Portal 필수 사용 원칙**:
   - `OfficialAttendanceSheet`는 반드시 `createPortal(content, document.body)`로 body 직속 자식으로 렌더링한다.
   - 이유: admin UI 레이아웃이 `visibility: hidden`으로 숨겨도 공간을 점유하여 빈 페이지가 발생하고, `position: fixed`는 인쇄 시 모든 페이지에 반복 렌더링되기 때문.
   - Print CSS는 `body > *:not(.oas-modal-overlay) { display: none }`으로 admin UI 레이아웃 공간까지 완전 제거하고, 모달 오버레이는 `position: static`으로 정상 흐름에 배치하여 `page-break-after`가 동작하도록 한다.
   - SSR 가드: `const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []);`로 클라이언트 마운트 후에만 portal 렌더링.

3. **출석부 상단 여백은 콘텐츠 내부 패딩으로만 조정**:
   - `position: absolute; top: Nmm` 방식으로 인쇄 영역 전체를 이동하면 `max-height`와의 합산 초과로 하단 행이 잘린다.
   - 제목 div의 `pt-[Nmm]`으로 내부 여백만 조정하고, 박스 크기는 건드리지 않는다.

4. **지도교사 영역 정렬 및 서명 배치 표준**:
   - 제목: 가운데 정렬(`text-center`).
   - 지도교사: 우측 정렬(`justify-end`), 각 강사 이름 바로 옆에 도장(`OfficialSeal`)을 쌍으로 배치.
   - 보조강사 구분 없이 전체를 `allInstructors = [mainInstructor, ...assistantInstructors]`로 통합하여 단일 라인에 나열.

## 방과후 강사 출근부 다중 강사 레이아웃 규칙

1. **출근부 열 폭 비율 표준 (4인 서명 가로 배치 보장)**:
   - `OfficialWorkRegister.tsx`의 출근부 표 열 폭은 차시 9% · 날짜 13% · 서명 28% (합계 50%, 좌우 동일).
   - 서명 칸 28% = 190mm 기준 53.2mm → 11mm 도장 4개 + 간격 4.5mm = 48.5mm → 한 줄 배치 가능.
   - 차시/날짜 폰트는 13px, 행 높이는 44px(11.6mm)로 유지하여 11mm 도장이 행을 넘치지 않도록 한다.

2. **강사 4명 이상 시 메타 정보 표 이름 2명씩 줄바꿈**:
   - `강사` 셀에서 `allCourseInstructors.length >= 4`이면 `slice(0,2)` 1행 + `slice(2)` 2행으로 분리 렌더링.
   - 3명 이하는 기존대로 한 줄 표기.

3. **`WorkRegisterSeal` 크기 표준**:
   - 이미지 서명: `maxWidth: 12mm, maxHeight: 11mm`.
   - 원형 직인: `width/height: 11mm`.
   - 출근부 행 높이 44px 기준 내에서 4인이 가로로 배치되도록 설계.

## 방과후 출석부 출결 기본값 및 자동화 규칙
1. **출결 상태 기본 '출석(○)' 자동 지정 및 전원출석 버튼 배제 원칙**:
   - 출석부 진입 시 대부분의 학생이 정상 출석하는 학교 현장 특성을 반영하여, 기록이 없는 학생은 기본 '출석(○)' 상태로 자동 초기화한다.
   - 단, 기존에 이미 저장된 출결 기록(결석, 지각, 조퇴, 개별하교, 기존 출석)은 절대 덮어쓰지 않고 완벽히 보존한다(`hasAnyRecord` 검증 후 미기록 회차만 초기화).
   - 기본값이 출석이므로 상단 '전원출석' 버튼은 UI에서 영구 제거하고, 교사는 결석/조퇴/개별하교 등 예외 학생만 원클릭/드롭다운으로 변경한다.
2. **다중 강좌/타 요일 수강생 출석 레코드 ID 강좌 격리 및 동시성 보장 원칙**:
   - 동일한 학생이 여러 강좌(예: 월/수 KIS 배구부, 화/목 다른 강좌 등)를 수강할 때 출석 문서 ID가 충돌하여 서로의 출석 체크를 덮어쓰거나 미체크로 초기화하는 현상을 원천 차단하기 위해, Firestore 문서 ID는 반드시 `att_${courseId}_${studentId}_s${sessionNo}` 형식을 표준화하여 사용한다.
   - 출석 변경 사항 저장 시 diff 연산 및 삭제 대상(`toDeleteIds`) 추출은 반드시 현재 조작 중인 강좌(`courseId`) 범위 내로 엄격히 한정하여 타 강좌 데이터가 삭제되지 않도록 보호한다.
   - 저장 중 Firestore 실시간 스냅샷 억제는 전역 플래그가 아닌 강좌 ID 단위(`isSavingCourseIdRef`)로 격리하여, 여러 강좌가 동시에 출석 체크를 진행하더라도 타 강좌의 실시간 변경 사항이 차단되거나 유실되지 않고 안전하게 동기화되도록 보장한다.

## 학급 목록 정렬 및 안전 비교 규칙
1. **학급 키 오름차순 정렬 및 유치원·특수 학년 안전 비교 원칙**:
   - 학급 선택 드롭다운(`availableClassKeys`, `myHomeroomKeys`) 정렬 시 단순 `Number(grade)` 파싱을 사용하면 유치원(예: `유치원국화반-1`) 등의 비숫자 문자열이 `NaN`을 반환하여 정렬 비교가 붕괴되고 5~6학년 사이에 유치원이 끼어들며 전체 정렬이 왜곡된다.
   - 반드시 유치원 계열은 `gradeRank = 0`(초등 1학년 앞)으로 가중치를 부여하고, 초등 1~6학년은 `1~6`, 중고등은 `7~12`로 명시적 순위를 부여하여 오름차순 정렬한다.

## 스쿨버스 담당 교사 배정 구조 (4대 체계 및 등하교 단일 배정 원칙)
1. **스쿨버스 담당 교사 4대 배정 카테고리**:
   - 스쿨버스 담당 교사는 `등하교(commute)`, `방과후(afterSchool)`, `토요일(saturday)`, `등교지도(morningGate)` 4개 카테고리로만 배정 및 운영된다.
2. **등하교 단일 배정 및 하교(Afternoon) 정본 원칙**:
   - 등교와 하교는 별개의 버스로 배정되지 않으며 동일한 버스를 동일 교사가 담당한다. 따라서 교사용 페이지(`/teacher/bus`)와 관리자 페이지(`/admin/bus`)에서 등교와 하교를 쪼개지 않고 '등하교' 단일 버스로 통합 관리 및 표출한다.
   - Firestore DB 상 과거 잔재로 인해 `Morning` 노선과 `Afternoon` 노선 간 교사 배정 데이터가 불일치할 경우, 항상 하교(`Afternoon`) 노선의 배정 데이터를 정본(Single Source of Truth)으로 삼아 등교 노선에 100% 동기화하며, 교사 정보 모달 및 UI 로딩 시에도 `Afternoon` 데이터를 최우선으로 바인딩한다.

## 학생 계정 관리 및 3대 컬렉션(users, master_students, kisbus) 완결 동기화 규칙
1. **학생 계정 `users` 문서 ID 표준화 (이메일 기반 고유 키)**:
   - `masterStudentService` 등에서 개별 학생 계정을 생성할 때 `doc(collection(db, 'users'))` 난수 ID 생성을 원천 차단한다.
   - 반드시 소문자 학생 이메일(`cleanEmail.toLowerCase()`)을 문서 ID로 지정하여 생성하며, `email, name, studentName, displayName, grade, studentGrade, class, studentClass, number, studentNumber, gender, role: 'student'` 등 일관된 필드명을 동시 저장하여 시스템 설정 및 디렉토리 조회 시 누락 및 미배정 현상을 방지한다.

2. **학생 성별(`gender`) 변경 보장 및 스쿨버스·체육(PAPS) 다방향 실시간 동기화**:
   - 학생의 성별 정보(`'Male' | 'Female'`)는 스쿨버스 좌석 배치 및 학교체육(PAPS) 남/여 기준표 판정에 핵심적인 데이터이므로, '개별 계정 추가' 및 '학생 정보 수정' 모달 모두에 성별 선택 UI(`Select`)를 필수로 제공한다.
   - 학생 정보 수정(`updateMasterStudent`) 시 `master_students` 컬렉션뿐만 아니라 메인 `users` 컬렉션과 스쿨버스 DB(`kisbus.students`)의 `gender` 필드를 동시에 양방향 동기화하여 데이터 불일치를 원천 차단한다.

3. **담임 출결 등록 시 스쿨버스 및 방과후 완결 연동 (복합 키 역추적)**:
   - 담임 교사가 결석/조퇴/인정결석을 등록할 때, 포털 학생 ID에만 의존하지 않고 스쿨버스 DB(`kisbus.students`)를 `이름 + 학년 + 반` 복합 키 및 학생 이메일로 반드시 역추적하여 실제 버스 학생 ID를 도출한다.
   - 역추적된 학생은 당일 스쿨버스 운행 노선의 `notBoarding` 목록에 자동 등록되어야 하며, 방과후 수강 명단(`afterschool_enrollments`)도 동일하게 조회하여 당일 방과후 출석부(`attendance`)에 자동으로 결석 사유가 동기화되어야 한다.

4. **방과후 수강 신청 시 학생 프로필 영구 보존 및 다자녀 오염 방지**:
   - 방과후 수강 신청 트랜잭션(`runAfterschoolEnrollmentTransaction`) 실행 시 학생의 학년(`grade`), 반(`classNum`), 번호(`studentNum`), 학생 이메일(`studentEmail`), 버스 신청 여부(`needsBus`)를 수강 문서에 반드시 영구 저장한다.
   - 학부모가 다자녀를 대리 신청할 때 부모 프로필이나 타 자녀의 정보로 덮어씌워지지 않도록 각 자녀별 독립 프로필을 명시적으로 바인딩한다.

5. **스쿨버스 미등록 학생의 방과후 하교 버스 자동 프로비저닝**:
   - 정규 등하교 버스를 이용하지 않는 학생이 방과후 하교 버스 탑승을 희망(`needsBus === true` 또는 `kisbusNo !== '-'`)한 경우, 스쿨버스 관리자 배정 탭에서 누락되지 않도록 `kisbus.students`에 기본 프로필을 안전하게 자동 생성(프로비저닝)하여 관리자가 즉시 좌석을 배정할 수 있도록 보장한다.

6. **통합 학생 마스터 계정 엑셀 일괄 등록 표준 양식 및 방과후/버스 연동 미리보기 검증**:
   - 통합 학생 마스터 엑셀 일괄 등록 시 표준 양식(`학년`, `반`, `번호`, `이름`, `영문이름(선택)`, `성별`, `계정(이메일)`, `학부모 연락처(선택)`)을 필수 제공한다.
   - 영문이름, 학부모 연락처 등 선택 항목이 비어있어도 정상 등록되어야 한다.
   - 엑셀 업로드 시 즉각 DB 반영 전 `학년 + 반 + 이름` 복합 키로 `afterschool_enrollments` 및 `kisbus.students`와 상호 대조하여 방과후 수강 강좌 및 스쿨버스 노선 연동 현황(연동 정보 또는 '없음')을 미리보기 테이블에 표시한다.
   - 계정 이메일 유효성 검사(`isStudentEmail`)를 통해 오류 행을 사전에 식별하고, 관리자가 미리보기 확인 후 최종 일괄 등록할 수 있도록 보장한다.

7. **학생 영문 이름(`nameEn`) 관리 및 이메일 기반 자동 추출과 영문 출석부 표출 보장**:
   - 학생 정보 수정 모달(학급 담임 및 관리자 마스터)과 신규 학생 등록 모달에 '학생 영문 이름(`nameEn`)' 입력 필드를 필수로 제공한다.
   - 기존 등록된 영문 이름이 있으면 해당 값을 표시하고, 영문 이름이 없는 경우에는 학생 이메일 계정(예: `2022kangsoobin@kshcm.net`)에서 입학년도(숫자 4자리)를 제외한 영문 문자열(`kangsoobin`)을 자동 추출(`extractEnglishNameFromEmail`)하여 기본값으로 입력 및 바인딩한다.
   - 언어 설정을 영문(`en`)으로 전환했을 때나 외부 강사용 공유 출석부에서 외국인 교사가 학생 이름을 읽지 못해 출결 체크에 지장이 생기지 않도록, 학생 이름을 영문 이름(`nameEn` 또는 추출된 영문명)으로 최우선 렌더링하고 한글 이름을 괄호로 병기한다.

8. **담임 교사 학생 정보 탭의 하교 버스 요일별 표시 및 직접 해제 기능 표준화**:
   - 담임 교사 페이지(`/teacher/homeroom`)의 학생 정보 확인 탭에서 스쿨버스 노선 컬럼은 `kisbus routes` 컬렉션(`type === 'Afternoon'`)을 실시간 구독(`onRoutesUpdate`)하여 **학생별 요일별 하교 버스 맵**을 계산한다.
   - 요일별로 버스가 동일하더라도 모든 배정 요일을 개별 뱃지(`[하교 월] 18호`, `[하교 화] 18호` 등)로 항상 표시한다. 조건부 통합 표시를 하면 안 된다.
   - 하교 버스 뱃지는 클릭 가능한 상태(`cursor-pointer`, hover 색상 변경)로 표시하며, 클릭 시 "유지/해제" 확인 Dialog를 열어 담임 교사가 해당 요일 하교 버스 탑승을 직접 미배정 처리(`unassignStudentFromAllRoutes(studentId, ['Afternoon'], dayOfWeek)`)할 수 있도록 한다.
   - 해제 즉시 `kisbus routes` 컬렉션이 업데이트되어 스쿨버스 관리자 페이지·교사 페이지와 실시간 공유된다.
   - 등교 버스는 담임이 조정하지 않으므로 해제 기능을 제공하지 않고, 방과후 버스도 방과후 전담 페이지에서 관리하므로 담임 페이지에서는 표시만 한다.

9. **방과후 강좌 강사진 최대 6명 동적 확장 및 UI 최적화 규칙**:
   - 방과후 강좌 정보 마스터 수정(`AdminPanel`) 및 강좌 관리(`CourseManagement`)에서 다수 강사 참여 강좌(오케스트라 등)를 원활히 지원하기 위해 강사진을 최대 6명(`instructorName`, `instructor2`~`instructor6`, `assistantTeachers`)까지 등록 및 관리할 수 있도록 확장한다.
   - 수정 모달 진입 시 불필요하게 팝업창이 길어지지 않도록 기본 2명(`강사 1(주강사)`, `강사 2(보조강사)`)만 노출하고, 기존에 등록된 강사 수에 맞춰 슬롯을 동적으로 펼친다.
   - '강사 추가' 버튼을 통해 3~6번째 강사 슬롯을 순차적으로 추가할 수 있으며, 추가된 슬롯에는 개별 '삭제' 버튼을 제공하여 강사명 초기화 및 슬롯 축소를 지원한다.
   - 보조강사 배정 모달(`CourseManagement`)에서도 최대 5명(주강사 포함 총 6명)까지 배정 및 저장(`instructor2~6`)할 수 있도록 상호 연동한다.
   - 공식 출석부(`OfficialAttendanceSheet`), 강사 출근부(`OfficialWorkRegister`), 사이드바 및 결재함 본인 강좌 필터링에서도 6명 전체 강사를 누락 없이 검증 및 표출한다.


## UI 컴포넌트 Radix Dialog + Popover 포커스 트랩 원칙

1. **Dialog 내부 Popover/Combobox 포커스 트랩 해결**:
   - Radix UI `Dialog` 컴포넌트 내부에서 `Popover` 기반 컴포넌트(Combobox, Command, DatePicker 등)를 사용할 때 Dialog의 포커스 트랩(focus trap)으로 인해 팝오버가 열리지 않는 현상이 발생할 수 있다.
   - 이를 해결하려면 `Popover` 컴포넌트에 `modal={true}` prop을 명시하여 팝오버가 독립적인 포커스 레이어에서 동작하도록 강제한다.
   - `Combobox` 등 공용 컴포넌트에는 `modal?: boolean` prop을 추가하고, Dialog 내부에서 사용 시 `modal={true}`로 전달한다.

## 통합 학생 마스터와 스쿨버스 DB 간 학적 완결 동기화 규칙

1. **학생 인적사항(학년/반/번호) 스쿨버스 DB 1:1 동기화 필수**:
   - 통합 학생 계정 관리(`/admin/students`)에서 학생 인적사항(학년, 반, 번호, 목적지, 연락처 등)을 저장하거나 수정할 때, 메인 DB(`master_students`, `users`)뿐만 아니라 kisbus DB의 `students` 컬렉션의 `grade`, `class`, `number`, `studentEmail`도 함께 즉각 1:1로 업데이트해야 한다.
   - 학년/반 불일치로 인해 스쿨버스 노선 배정, 방과후 출석부, 수강료 정산 간 데이터 단절 및 미배정 오탐이 발생하는 것을 원천 차단한다.

2. **방과후 수강 명단과 스쿨버스 간 매칭 시 이메일 최우선 및 고유 학생 구제**:
   - 방과후 수강 명단(`StudentManagement.tsx`), 스쿨버스 관리자(`/admin/bus`), 하교 버스 연동(`assignments.ts`) 등에서 학생을 상호 매칭할 때 `studentEmail`을 최우선 고유 키로 활용한다.
   - `studentId`가 비어있거나 반(`class` vs `classNum`) 정보에 오차/학적 변동이 있더라도, 동일 학년 내 동명이인이 없는 고유 학생인 경우 안전하게 매칭하여 목적지 누락 및 버스 미배정 표시를 원천 방지한다.

## 학교 체육(PAPS) 담임 교사 접근 권한 및 학급 데이터 자동 격리 규칙

1. **학급 담임 교사의 체육 시스템 자동 접근 허용 (Zero-Configuration)**:
   - 학교 체육 시스템(`/teacher/pe`)은 체육 전담 교사뿐만 아니라 학교 조직도(`homerooms`)에 등록된 학급 담임 교사의 접근을 기본 허용한다. 관리자가 별도로 체육 권한을 일일이 부여하지 않아도 담임 교사로 등록되어 있으면 시스템이 즉시 접근을 허가한다.

2. **담임 교사 접속 시 본인 학급 데이터 자동 격리 및 모드 최적화**:
   - 담임 교사가 접속한 경우, 전교 체육 대회 기획이나 종목 설정 등 전교 관리자 메뉴는 제한하고, 해당 교사의 담당 학급(예: 3학년 2반) 학생들로 데이터를 자동 필터링한다.
   - 상단 헤더에 `[O학년 O반 담임 교사]` 뱃지를 표출하며, 측정 기록 조회(`RecordBrowser`), 학생별 성장 분석(`ClassAnalytics`), 기록 입력(`RecordInput`), 명예의 전당(`Ranking`), 학급 팀 밸런서(`TeamBalancer`) 등 학급 지도에 필요한 핵심 기능을 편리하게 이용할 수 있도록 보장한다.

## 담임 교사 학급 관리(과제/기록/알림) 및 교실 운영 규칙

1. **통합 학생 마스터 기반 담임 학급 학생 데이터 엄격 격리 바인딩**:
   - 담임 업무 관리소(`/teacher/homeroom`)의 '학급 관리' 탭은 통합 학생 계정 마스터(`master_students`)를 기준으로 현재 선택된 담임 학급 학생(`classStudents`)만 엄격하게 연결한다.
   - 담임 교사는 본인 담당 학급만 기본 선택 및 접근 가능하며, 전교 권한자(시스템 설정/학생 출결 담당자)에 한해 드롭다운으로 전교 모든 학급을 전환 모니터링할 수 있도록 한다.

2. **교실 칠판 알림장 및 숙제 전자칠판 프레젠테이션 모드 표준**:
   - 일일 칠판 알림장은 오늘 날짜(`todayStr`) 기준 Firestore(`homeroom_classes/{classKey}/memos/{date}`)에 실시간 저장하며, [전자칠판 전체화면] 모드를 제공하여 교실 빔프로젝터/TV에서 대형 폰트로 아침 알림판을 상시 띄워둘 수 있도록 한다.
   - 전자칠판 전체화면 모드는 교실 조명 및 교사 선호에 맞추어 **[칠판 모드]**(짙은 초록 전통 흑판 + 노란색 분필 폰트 + 고채도 노란색 날짜)와 **[화이트보드 모드]**(깨끗한 화이트보드 + 짙은 네이비 마커 폰트)를 1터치로 즉시 전환할 수 있도록 제공하고, 선택 상태는 `localStorage`에 자동 영구 보존한다.
   - 배경 투과로 인한 글자 겹침을 방지하기 위해 전체화면 모달은 반드시 `z-[9999]`와 100% 완전 불투명 단색 배경(`opacity: 1`)을 적용하며, 닫기(`ESC`) 버튼 및 저장 버튼은 배경색과 강하게 대비되는 전용 고대비 스타일(`red-500/20` 테두리 강조 / 고채도 `emerald`)을 필수로 적용한다.
   - 숙제 확인은 번호/이름 타일 원터치 체크로 진행률과 미제출자 명단을 실시간 집계하며, [칠판 뷰] 및 [전체 미제출 칠판 띄우기] 프레젠테이션 모드를 통해 미제출 학생 카드만 교실 화면에 크게 노출하고 검사 시 클릭하여 즉각 사라지는 실시간 피드백을 제공한다.

3. **행동 관찰 기록, 보호자 상담 메모 분리 및 AI 프롬프트/엑셀 지원**:
   - 학생 타일 클릭 시 행동 관찰 기록을 빠르게 입력(`Ctrl + Enter` 즉시 저장)할 수 있도록 모달을 제공하고 학생별 누적 기록 타임라인을 관리한다.
   - 생활기록부 및 학부모 정기 상담 편의를 위해 원터치 기록 복사, AI 종합의견 프롬프트 자동 복사(`handleCopyAIPrompt`), 월별 매트릭스 표 및 엑셀 다운로드(`xlsx`)를 표준 지원한다.

4. **학급 교실 관리(`homeroom_classes`) Firestore 보안 규칙 및 권한 보장**:
   - 칠판 알림 메모(`memos`), 숙제(`homeworks`), 숙제 제출 체크(`hw_checks`), 행동 관찰(`behavior_records`), 상담 일지(`consultations`)가 저장되는 `homeroom_classes/{document=**}` 경로는 담임 교사뿐만 아니라 관리자/시스템 설정 담당자도 저장 및 열람할 수 있도록 `firestore.rules`에 `allow read, write: if isSignedIn();` 규칙을 필수로 보장한다.

## Google Drive 중앙 저장소 학년별 수업자료 공유 체계 및 담임 관리소 연동 규칙

1. **표준 학년별 수업자료 공유 폴더 계층 구조 (1~6학년)**:
## 실무 전담 계정(직책: '담당')의 진입 경로 격리 및 리다이렉트 보장 규칙
1. **전담 계정 로그인 리다이렉트 및 관리자 지정 연동**:
   - 전자결재나 일반 교원 기능을 사용하지 않고 특정 현장 업무(스쿨버스 등)만 전담하는 계정은 `UserProfile.loginRedirectUrl` 필드를 통해 로그인 직후 전용 페이지로 즉시 자동 이동시킨다.
   - 시스템 설정의 교직원 관리 탭에서 직책이 '담당'인 계정을 추가하거나 편집할 때, 로그인 후 이동할 목적지 페이지(`/admin/bus`, `/teacher/bus` 등)를 지정할 수 있는 UI를 필수로 제공한다.
   - `bus@kshcm.net` 등 표준 시스템 전담 계정은 DB 상에 명시적 URL 필드가 누락되어 있더라도 로그인 시 기본 목적지(`/admin/bus`)로 자동 보정 및 저장하여 사용자 조작 누락에 따른 혼선을 원천 방지한다.
2. **직책 '담당'의 사이드바 원천 비활성화 및 접힘 상태 디폴트 고정**:
   - 직책이 '담당'(`profile?.role === '담당'`)인 사용자는 전자결재 사이드바를 전혀 사용하지 않으므로, `SidebarProvider`에서 로컬스토리지 저장 여부와 무관하게 `isSidebarOpen: false`를 디폴트로 고정하고 토글 기능을 차단한다.
   - 상단 `AppHeader`에서도 담당 직책 사용자에게는 사이드바 열기/접기 버튼을 렌더링하지 않아 온전한 전용 화면 공간을 확보한다.

## 스쿨버스 관리자 레이아웃 스크롤 컨테이너 및 헤더 Sticky 고정 규칙
1. **내부 스크롤 격리 및 탭/필터 헤더 무이탈 고정 (`sticky top-0`)**:
   - 스쿨버스 관리자(`/admin/bus`) 등 대량의 학생 카드와 좌석표를 스크롤하는 대형 관리자 화면은 `MainLayout`에 `contentClassName="p-0 h-full overflow-y-auto"`를 부여하여 상단 스쿨버스 헤더 아래 본문 영역이 독립 스크롤 컨테이너가 되도록 격리한다.
   - 내부의 버스 탭 및 요일/경로/정류장 필터 래퍼는 `sticky top-0`으로 설정하여, 학생 카드나 미배정 목록을 아래로 길게 스크롤하더라도 상단 헤더 및 필터 바가 뷰포트 밖으로 밀려 올라가거나 해제되지 않고 상단에 완벽히 고정되도록 보장한다.

## 대시보드 주요 업무 바로가기 교직원 역할별 격리 및 동적 프리셋 규칙
1. **역할/담당 기반 선택 목록 엄격 격리**:
   - 대시보드(`/inbox`) 상단 주요 업무 바로가기 및 설정 모달(`MajorTasksModal`)에서는 모든 교사에게 전체 업무나 특정 업무를 무조건 나열하지 않는다.
   - 반드시 로그인한 교사의 실제 담당 직책/권한(담임, 방과후 강좌, 스쿨버스 탑승/관리, 체육, 보건, 복무 등)에 해당하는 업무(`availableTasks`)만 선택 후보로 제공하여, 본인의 업무가 아닌 기능이 노출되거나 선택되는 것을 원천 차단한다.
2. **계정별 로컬 저장소 격리 및 하드코딩 프리셋 배제**:
   - 주요 업무 바로가기 설정은 브라우저 공용 키가 아닌 사용자 이메일별 고유 키(`kis_dashboard_major_tasks_v2_${email}`)로 분리 저장한다.
   - 특정 업무(방과후/스쿨버스 등)를 일괄 기본값으로 강제하지 않으며, 저장된 값이 없을 때는 해당 사용자가 실제로 담당하고 있는 업무 중 상위 최대 3개를 기본값으로 자동 산출한다.

## 방과후 및 스쿨버스 '강사' 직책의 접근 권한 및 데이터 엄격 격리 규칙
1. **직책 '강사'의 접근 범위 한정 및 라우팅 격리**:
   - 교직원 직책 중 '강사'(`role === '강사'`)는 오직 **본인의 방과후 출석부(`/teacher/afterschool`)**와 **스쿨버스 탑승 관리(`/teacher/bus`)**에만 접근 권한을 갖는다.
   - 전자결재(`/inbox`, `/sent`, `/pending` 등), 교원 서비스(복무, 초과근무, 보결), 학생 계정, 학교 체육, 보건 등 타 업무 및 기안 작성(`/new`) 화면은 사이드바, 모바일 하단바, 레이아웃(`(app)/layout.tsx`, `MainLayout`) 레벨에서 원천 숨김 및 차단한다.
   - 로그인 직후 및 비인가 경로 직접 URL 입력 시 기본 목적지인 `/teacher/afterschool`로 자동 리다이렉트한다.
2. **방과후 강좌 및 출석부의 본인 담당 강좌(`myOwnCourses`) 엄격 제한**:
   - 강사 직책인 사용자가 방과후 교사 페이지(`/teacher/afterschool`)에 진입할 경우, 일반 교사와 달리 강좌 목록(`teacherCourses`)에 전체 강좌 fallback을 절대 허용하지 않고 오직 본인이 주강사/추가강사(1~4)/보조강사로 배정된 강좌(`myOwnCourses`)만 반환한다.
   - 타 강사의 강좌, 수강생 명단 및 출석부 데이터가 노출되거나 수정되는 것을 원천 차단한다.

## 시스템 관리자 권한 토글 및 전담 계정 격리 규칙
1. **관리자 권한 강제 주입(Hardcoded Admin)의 슈퍼 관리자 한정 원칙**:
   - `auth-provider.tsx` 등 전역 인증 프로필 로더에서 `isAdmin: true`를 강제 주입하는 대상은 오직 최상위 슈퍼 관리자(`beside1s@kshcm.net`)로 한정한다.
   - 현장 전담 계정(`bus@kshcm.net` 등)에 `isHardcodedAdmin = true`를 부여하여 관리자가 시스템 설정에서 관리자 토글 스위치를 꺼도 다시 켜지게 만드는 하드코딩을 원천 차단한다.
2. **관리자 토글 변경 시 세션 상태 실시간 동기화**:
   - 시스템 설정의 사용자 관리 탭에서 특정 계정의 `isAdmin` 스위치를 변경할 때, 현재 로그인한 사용자 본인의 계정이면 `updateProfile({ isAdmin: checked })`를 즉시 호출하여 헤더의 설정 톱니바퀴 아이콘 및 관리자 메뉴가 페이지 새로고침 없이 즉각 동기화되도록 보장한다.
   - 설정 모달 열림 시(`isOpen === true`) 사용자 목록(`fetchUsers(true)`)을 강제 무효화하여 최신 Firestore 값을 온전히 반영한다.

## 방과후 공식 출석부 인쇄 레이아웃 규칙

1. **30명 초과 시 2페이지 분리 원칙 (잘림 원천 차단)**:
   - `OfficialAttendanceSheet.tsx`는 `students.length > 30`(ROWS_PER_PAGE)일 때 자동으로 2페이지로 분리한다.
   - 1페이지(1~30번), 2페이지(31번~)에 동일한 제목 + 지도교사 영역을 렌더링하고, 일련번호는 연속으로 이어진다.
   - CSS: `attendance-page-block { height: 277mm; page-break-after: always }`, 마지막 블록은 `page-break-after: avoid`.
   - `overflow: hidden + max-height` 구속은 단일 페이지 전용 클래스(`attendance-page-single`)에만 적용하며, 다중 페이지 래퍼에는 `overflow: visible`을 적용한다.

2. **출석부 인쇄 시 React Portal 필수 사용 원칙**:
   - `OfficialAttendanceSheet`는 반드시 `createPortal(content, document.body)`로 body 직속 자식으로 렌더링한다.
   - 이유: admin UI 레이아웃이 `visibility: hidden`으로 숨겨도 공간을 점유하여 빈 페이지가 발생하고, `position: fixed`는 인쇄 시 모든 페이지에 반복 렌더링되기 때문.
   - Print CSS는 `body > *:not(.oas-modal-overlay) { display: none }`으로 admin UI 레이아웃 공간까지 완전 제거하고, 모달 오버레이는 `position: static`으로 정상 흐름에 배치하여 `page-break-after`가 동작하도록 한다.
   - SSR 가드: `const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []);`로 클라이언트 마운트 후에만 portal 렌더링.

3. **출석부 상단 여백은 콘텐츠 내부 패딩으로만 조정**:
   - `position: absolute; top: Nmm` 방식으로 인쇄 영역 전체를 이동하면 `max-height`와의 합산 초과로 하단 행이 잘린다.
   - 제목 div의 `pt-[Nmm]`으로 내부 여백만 조정하고, 박스 크기는 건드리지 않는다.

4. **지도교사 영역 정렬 및 서명 배치 표준**:
   - 제목: 가운데 정렬(`text-center`).
   - 지도교사: 우측 정렬(`justify-end`), 각 강사 이름 바로 옆에 도장(`OfficialSeal`)을 쌍으로 배치.
   - 보조강사 구분 없이 전체를 `allInstructors = [mainInstructor, ...assistantInstructors]`로 통합하여 단일 라인에 나열.

## 방과후 강사 출근부 다중 강사 레이아웃 규칙

1. **출근부 열 폭 비율 표준 (4인 서명 가로 배치 보장)**:
   - `OfficialWorkRegister.tsx`의 출근부 표 열 폭은 차시 9% · 날짜 13% · 서명 28% (합계 50%, 좌우 동일).
   - 서명 칸 28% = 190mm 기준 53.2mm → 11mm 도장 4개 + 간격 4.5mm = 48.5mm → 한 줄 배치 가능.
   - 차시/날짜 폰트는 13px, 행 높이는 44px(11.6mm)로 유지하여 11mm 도장이 행을 넘치지 않도록 한다.

2. **강사 4명 이상 시 메타 정보 표 이름 2명씩 줄바꿈**:
   - `강사` 셀에서 `allCourseInstructors.length >= 4`이면 `slice(0,2)` 1행 + `slice(2)` 2행으로 분리 렌더링.
   - 3명 이하는 기존대로 한 줄 표기.

3. **`WorkRegisterSeal` 크기 표준**:
   - 이미지 서명: `maxWidth: 12mm, maxHeight: 11mm`.
   - 원형 직인: `width/height: 11mm`.
   - 출근부 행 높이 44px 기준 내에서 4인이 가로로 배치되도록 설계.

## 방과후 출석부 출결 기본값 및 자동화 규칙
1. **출결 상태 기본 '출석(○)' 자동 지정 및 전원출석 버튼 배제 원칙**:
   - 출석부 진입 시 대부분의 학생이 정상 출석하는 학교 현장 특성을 반영하여, 기록이 없는 학생은 기본 '출석(○)' 상태로 자동 초기화한다.
   - 단, 기존에 이미 저장된 출결 기록(결석, 지각, 조퇴, 개별하교, 기존 출석)은 절대 덮어쓰지 않고 완벽히 보존한다(`hasAnyRecord` 검증 후 미기록 회차만 초기화).
   - 기본값이 출석이므로 상단 '전원출석' 버튼은 UI에서 영구 제거하고, 교사는 결석/조퇴/개별하교 등 예외 학생만 원클릭/드롭다운으로 변경한다.
2. **다중 강좌/타 요일 수강생 출석 레코드 ID 강좌 격리 및 동시성 보장 원칙**:
   - 동일한 학생이 여러 강좌(예: 월/수 KIS 배구부, 화/목 다른 강좌 등)를 수강할 때 출석 문서 ID가 충돌하여 서로의 출석 체크를 덮어쓰거나 미체크로 초기화하는 현상을 원천 차단하기 위해, Firestore 문서 ID는 반드시 `att_${courseId}_${studentId}_s${sessionNo}` 형식을 표준화하여 사용한다.
   - 출석 변경 사항 저장 시 diff 연산 및 삭제 대상(`toDeleteIds`) 추출은 반드시 현재 조작 중인 강좌(`courseId`) 범위 내로 엄격히 한정하여 타 강좌 데이터가 삭제되지 않도록 보호한다.
   - 저장 중 Firestore 실시간 스냅샷 억제는 전역 플래그가 아닌 강좌 ID 단위(`isSavingCourseIdRef`)로 격리하여, 여러 강좌가 동시에 출석 체크를 진행하더라도 타 강좌의 실시간 변경 사항이 차단되거나 유실되지 않고 안전하게 동기화되도록 보장한다.

## 학급 목록 정렬 및 안전 비교 규칙
1. **학급 키 오름차순 정렬 및 유치원·특수 학년 안전 비교 원칙**:
   - 학급 선택 드롭다운(`availableClassKeys`, `myHomeroomKeys`) 정렬 시 단순 `Number(grade)` 파싱을 사용하면 유치원(예: `유치원국화반-1`) 등의 비숫자 문자열이 `NaN`을 반환하여 정렬 비교가 붕괴되고 5~6학년 사이에 유치원이 끼어들며 전체 정렬이 왜곡된다.
   - 반드시 유치원 계열은 `gradeRank = 0`(초등 1학년 앞)으로 가중치를 부여하고, 초등 1~6학년은 `1~6`, 중고등은 `7~12`로 명시적 순위를 부여하여 오름차순 정렬한다.

## 스쿨버스 담당 교사 배정 구조 (4대 체계 및 등하교 단일 배정 원칙)
1. **스쿨버스 담당 교사 4대 배정 카테고리**:
   - 스쿨버스 담당 교사는 `등하교(commute)`, `방과후(afterSchool)`, `토요일(saturday)`, `등교지도(morningGate)` 4개 카테고리로만 배정 및 운영된다.
2. **등하교 단일 배정 및 하교(Afternoon) 정본 원칙**:
   - 등교와 하교는 별개의 버스로 배정되지 않으며 동일한 버스를 동일 교사가 담당한다. 따라서 교사용 페이지(`/teacher/bus`)와 관리자 페이지(`/admin/bus`)에서 등교와 하교를 쪼개지 않고 '등하교' 단일 버스로 통합 관리 및 표출한다.
   - Firestore DB 상 과거 잔재로 인해 `Morning` 노선과 `Afternoon` 노선 간 교사 배정 데이터가 불일치할 경우, 항상 하교(`Afternoon`) 노선의 배정 데이터를 정본(Single Source of Truth)으로 삼아 등교 노선에 100% 동기화하며, 교사 정보 모달 및 UI 로딩 시에도 `Afternoon` 데이터를 최우선으로 바인딩한다.

## 학생 계정 관리 및 3대 컬렉션(users, master_students, kisbus) 완결 동기화 규칙
1. **학생 계정 `users` 문서 ID 표준화 (이메일 기반 고유 키)**:
   - `masterStudentService` 등에서 개별 학생 계정을 생성할 때 `doc(collection(db, 'users'))` 난수 ID 생성을 원천 차단한다.
   - 반드시 소문자 학생 이메일(`cleanEmail.toLowerCase()`)을 문서 ID로 지정하여 생성하며, `email, name, studentName, displayName, grade, studentGrade, class, studentClass, number, studentNumber, gender, role: 'student'` 등 일관된 필드명을 동시 저장하여 시스템 설정 및 디렉토리 조회 시 누락 및 미배정 현상을 방지한다.

2. **학생 성별(`gender`) 변경 보장 및 스쿨버스·체육(PAPS) 다방향 실시간 동기화**:
   - 학생의 성별 정보(`'Male' | 'Female'`)는 스쿨버스 좌석 배치 및 학교체육(PAPS) 남/여 기준표 판정에 핵심적인 데이터이므로, '개별 계정 추가' 및 '학생 정보 수정' 모달 모두에 성별 선택 UI(`Select`)를 필수로 제공한다.
   - 학생 정보 수정(`updateMasterStudent`) 시 `master_students` 컬렉션뿐만 아니라 메인 `users` 컬렉션과 스쿨버스 DB(`kisbus.students`)의 `gender` 필드를 동시에 양방향 동기화하여 데이터 불일치를 원천 차단한다.

3. **담임 출결 등록 시 스쿨버스 및 방과후 완결 연동 (복합 키 역추적)**:
   - 담임 교사가 결석/조퇴/인정결석을 등록할 때, 포털 학생 ID에만 의존하지 않고 스쿨버스 DB(`kisbus.students`)를 `이름 + 학년 + 반` 복합 키 및 학생 이메일로 반드시 역추적하여 실제 버스 학생 ID를 도출한다.
   - 역추적된 학생은 당일 스쿨버스 운행 노선의 `notBoarding` 목록에 자동 등록되어야 하며, 방과후 수강 명단(`afterschool_enrollments`)도 동일하게 조회하여 당일 방과후 출석부(`attendance`)에 자동으로 결석 사유가 동기화되어야 한다.

4. **방과후 수강 신청 시 학생 프로필 영구 보존 및 다자녀 오염 방지**:
   - 방과후 수강 신청 트랜잭션(`runAfterschoolEnrollmentTransaction`) 실행 시 학생의 학년(`grade`), 반(`classNum`), 번호(`studentNum`), 학생 이메일(`studentEmail`), 버스 신청 여부(`needsBus`)를 수강 문서에 반드시 영구 저장한다.
   - 학부모가 다자녀를 대리 신청할 때 부모 프로필이나 타 자녀의 정보로 덮어씌워지지 않도록 각 자녀별 독립 프로필을 명시적으로 바인딩한다.

5. **스쿨버스 미등록 학생의 방과후 하교 버스 자동 프로비저닝**:
   - 정규 등하교 버스를 이용하지 않는 학생이 방과후 하교 버스 탑승을 희망(`needsBus === true` 또는 `kisbusNo !== '-'`)한 경우, 스쿨버스 관리자 배정 탭에서 누락되지 않도록 `kisbus.students`에 기본 프로필을 안전하게 자동 생성(프로비저닝)하여 관리자가 즉시 좌석을 배정할 수 있도록 보장한다.

6. **통합 학생 마스터 계정 엑셀 일괄 등록 표준 양식 및 방과후/버스 연동 미리보기 검증**:
   - 통합 학생 마스터 엑셀 일괄 등록 시 표준 양식(`학년`, `반`, `번호`, `이름`, `영문이름(선택)`, `성별`, `계정(이메일)`, `학부모 연락처(선택)`)을 필수 제공한다.
   - 영문이름, 학부모 연락처 등 선택 항목이 비어있어도 정상 등록되어야 한다.
   - 엑셀 업로드 시 즉각 DB 반영 전 `학년 + 반 + 이름` 복합 키로 `afterschool_enrollments` 및 `kisbus.students`와 상호 대조하여 방과후 수강 강좌 및 스쿨버스 노선 연동 현황(연동 정보 또는 '없음')을 미리보기 테이블에 표시한다.
   - 계정 이메일 유효성 검사(`isStudentEmail`)를 통해 오류 행을 사전에 식별하고, 관리자가 미리보기 확인 후 최종 일괄 등록할 수 있도록 보장한다.

7. **학생 영문 이름(`nameEn`) 관리 및 이메일 기반 자동 추출과 영문 출석부 표출 보장**:
   - 학생 정보 수정 모달(학급 담임 및 관리자 마스터)과 신규 학생 등록 모달에 '학생 영문 이름(`nameEn`)' 입력 필드를 필수로 제공한다.
   - 기존 등록된 영문 이름이 있으면 해당 값을 표시하고, 영문 이름이 없는 경우에는 학생 이메일 계정(예: `2022kangsoobin@kshcm.net`)에서 입학년도(숫자 4자리)를 제외한 영문 문자열(`kangsoobin`)을 자동 추출(`extractEnglishNameFromEmail`)하여 기본값으로 입력 및 바인딩한다.
   - 언어 설정을 영문(`en`)으로 전환했을 때나 외부 강사용 공유 출석부에서 외국인 교사가 학생 이름을 읽지 못해 출결 체크에 지장이 생기지 않도록, 학생 이름을 영문 이름(`nameEn` 또는 추출된 영문명)으로 최우선 렌더링하고 한글 이름을 괄호로 병기한다.

8. **담임 교사 학생 정보 탭의 하교 버스 요일별 표시 및 직접 해제 기능 표준화**:
   - 담임 교사 페이지(`/teacher/homeroom`)의 학생 정보 확인 탭에서 스쿨버스 노선 컬럼은 `kisbus routes` 컬렉션(`type === 'Afternoon'`)을 실시간 구독(`onRoutesUpdate`)하여 **학생별 요일별 하교 버스 맵**을 계산한다.
   - 요일별로 버스가 동일하더라도 모든 배정 요일을 개별 뱃지(`[하교 월] 18호`, `[하교 화] 18호` 등)로 항상 표시한다. 조건부 통합 표시를 하면 안 된다.
   - 하교 버스 뱃지는 클릭 가능한 상태(`cursor-pointer`, hover 색상 변경)로 표시하며, 클릭 시 "유지/해제" 확인 Dialog를 열어 담임 교사가 해당 요일 하교 버스 탑승을 직접 미배정 처리(`unassignStudentFromAllRoutes(studentId, ['Afternoon'], dayOfWeek)`)할 수 있도록 한다.
   - 해제 즉시 `kisbus routes` 컬렉션이 업데이트되어 스쿨버스 관리자 페이지·교사 페이지와 실시간 공유된다.
   - 등교 버스는 담임이 조정하지 않으므로 해제 기능을 제공하지 않고, 방과후 버스도 방과후 전담 페이지에서 관리하므로 담임 페이지에서는 표시만 한다.

9. **방과후 강좌 강사진 최대 6명 동적 확장 및 UI 최적화 규칙**:
   - 방과후 강좌 정보 마스터 수정(`AdminPanel`) 및 강좌 관리(`CourseManagement`)에서 다수 강사 참여 강좌(오케스트라 등)를 원활히 지원하기 위해 강사진을 최대 6명(`instructorName`, `instructor2`~`instructor6`, `assistantTeachers`)까지 등록 및 관리할 수 있도록 확장한다.
   - 수정 모달 진입 시 불필요하게 팝업창이 길어지지 않도록 기본 2명(`강사 1(주강사)`, `강사 2(보조강사)`)만 노출하고, 기존에 등록된 강사 수에 맞춰 슬롯을 동적으로 펼친다.
   - '강사 추가' 버튼을 통해 3~6번째 강사 슬롯을 순차적으로 추가할 수 있으며, 추가된 슬롯에는 개별 '삭제' 버튼을 제공하여 강사명 초기화 및 슬롯 축소를 지원한다.
   - 보조강사 배정 모달(`CourseManagement`)에서도 최대 5명(주강사 포함 총 6명)까지 배정 및 저장(`instructor2~6`)할 수 있도록 상호 연동한다.
   - 공식 출석부(`OfficialAttendanceSheet`), 강사 출근부(`OfficialWorkRegister`), 사이드바 및 결재함 본인 강좌 필터링에서도 6명 전체 강사를 누락 없이 검증 및 표출한다.


## UI 컴포넌트 Radix Dialog + Popover 포커스 트랩 원칙

1. **Dialog 내부 Popover/Combobox 포커스 트랩 해결**:
   - Radix UI `Dialog` 컴포넌트 내부에서 `Popover` 기반 컴포넌트(Combobox, Command, DatePicker 등)를 사용할 때 Dialog의 포커스 트랩(focus trap)으로 인해 팝오버가 열리지 않는 현상이 발생할 수 있다.
   - 이를 해결하려면 `Popover` 컴포넌트에 `modal={true}` prop을 명시하여 팝오버가 독립적인 포커스 레이어에서 동작하도록 강제한다.
   - `Combobox` 등 공용 컴포넌트에는 `modal?: boolean` prop을 추가하고, Dialog 내부에서 사용 시 `modal={true}`로 전달한다.

## 통합 학생 마스터와 스쿨버스 DB 간 학적 완결 동기화 규칙

1. **학생 인적사항(학년/반/번호) 스쿨버스 DB 1:1 동기화 필수**:
   - 통합 학생 계정 관리(`/admin/students`)에서 학생 인적사항(학년, 반, 번호, 목적지, 연락처 등)을 저장하거나 수정할 때, 메인 DB(`master_students`, `users`)뿐만 아니라 kisbus DB의 `students` 컬렉션의 `grade`, `class`, `number`, `studentEmail`도 함께 즉각 1:1로 업데이트해야 한다.
   - 학년/반 불일치로 인해 스쿨버스 노선 배정, 방과후 출석부, 수강료 정산 간 데이터 단절 및 미배정 오탐이 발생하는 것을 원천 차단한다.

2. **방과후 수강 명단과 스쿨버스 간 매칭 시 이메일 최우선 및 고유 학생 구제**:
   - 방과후 수강 명단(`StudentManagement.tsx`), 스쿨버스 관리자(`/admin/bus`), 하교 버스 연동(`assignments.ts`) 등에서 학생을 상호 매칭할 때 `studentEmail`을 최우선 고유 키로 활용한다.
   - `studentId`가 비어있거나 반(`class` vs `classNum`) 정보에 오차/학적 변동이 있더라도, 동일 학년 내 동명이인이 없는 고유 학생인 경우 안전하게 매칭하여 목적지 누락 및 버스 미배정 표시를 원천 방지한다.

## 학교 체육(PAPS) 담임 교사 접근 권한 및 학급 데이터 자동 격리 규칙

1. **학급 담임 교사의 체육 시스템 자동 접근 허용 (Zero-Configuration)**:
   - 학교 체육 시스템(`/teacher/pe`)은 체육 전담 교사뿐만 아니라 학교 조직도(`homerooms`)에 등록된 학급 담임 교사의 접근을 기본 허용한다. 관리자가 별도로 체육 권한을 일일이 부여하지 않아도 담임 교사로 등록되어 있으면 시스템이 즉시 접근을 허가한다.

2. **담임 교사 접속 시 본인 학급 데이터 자동 격리 및 모드 최적화**:
   - 담임 교사가 접속한 경우, 전교 체육 대회 기획이나 종목 설정 등 전교 관리자 메뉴는 제한하고, 해당 교사의 담당 학급(예: 3학년 2반) 학생들로 데이터를 자동 필터링한다.
   - 상단 헤더에 `[O학년 O반 담임 교사]` 뱃지를 표출하며, 측정 기록 조회(`RecordBrowser`), 학생별 성장 분석(`ClassAnalytics`), 기록 입력(`RecordInput`), 명예의 전당(`Ranking`), 학급 팀 밸런서(`TeamBalancer`) 등 학급 지도에 필요한 핵심 기능을 편리하게 이용할 수 있도록 보장한다.

## 담임 교사 학급 관리(과제/기록/알림) 및 교실 운영 규칙

1. **통합 학생 마스터 기반 담임 학급 학생 데이터 엄격 격리 바인딩**:
   - 담임 업무 관리소(`/teacher/homeroom`)의 '학급 관리' 탭은 통합 학생 계정 마스터(`master_students`)를 기준으로 현재 선택된 담임 학급 학생(`classStudents`)만 엄격하게 연결한다.
   - 담임 교사는 본인 담당 학급만 기본 선택 및 접근 가능하며, 전교 권한자(시스템 설정/학생 출결 담당자)에 한해 드롭다운으로 전교 모든 학급을 전환 모니터링할 수 있도록 한다.

2. **교실 칠판 알림장 및 숙제 전자칠판 프레젠테이션 모드 표준**:
   - 일일 칠판 알림장은 오늘 날짜(`todayStr`) 기준 Firestore(`homeroom_classes/{classKey}/memos/{date}`)에 실시간 저장하며, [전자칠판 전체화면] 모드를 제공하여 교실 빔프로젝터/TV에서 대형 폰트로 아침 알림판을 상시 띄워둘 수 있도록 한다.
   - 전자칠판 전체화면 모드는 교실 조명 및 교사 선호에 맞추어 **[칠판 모드]**(짙은 초록 전통 흑판 + 노란색 분필 폰트 + 고채도 노란색 날짜)와 **[화이트보드 모드]**(깨끗한 화이트보드 + 짙은 네이비 마커 폰트)를 1터치로 즉시 전환할 수 있도록 제공하고, 선택 상태는 `localStorage`에 자동 영구 보존한다.
   - 배경 투과로 인한 글자 겹침을 방지하기 위해 전체화면 모달은 반드시 `z-[9999]`와 100% 완전 불투명 단색 배경(`opacity: 1`)을 적용하며, 닫기(`ESC`) 버튼 및 저장 버튼은 배경색과 강하게 대비되는 전용 고대비 스타일(`red-500/20` 테두리 강조 / 고채도 `emerald`)을 필수로 적용한다.
   - 숙제 확인은 번호/이름 타일 원터치 체크로 진행률과 미제출자 명단을 실시간 집계하며, [칠판 뷰] 및 [전체 미제출 칠판 띄우기] 프레젠테이션 모드를 통해 미제출 학생 카드만 교실 화면에 크게 노출하고 검사 시 클릭하여 즉각 사라지는 실시간 피드백을 제공한다.

3. **행동 관찰 기록, 보호자 상담 메모 분리 및 AI 프롬프트/엑셀 지원**:
   - 학생 타일 클릭 시 행동 관찰 기록을 빠르게 입력(`Ctrl + Enter` 즉시 저장)할 수 있도록 모달을 제공하고 학생별 누적 기록 타임라인을 관리한다.
   - 생활기록부 및 학부모 정기 상담 편의를 위해 원터치 기록 복사, AI 종합의견 프롬프트 자동 복사(`handleCopyAIPrompt`), 월별 매트릭스 표 및 엑셀 다운로드(`xlsx`)를 표준 지원한다.

4. **학급 교실 관리(`homeroom_classes`) Firestore 보안 규칙 및 권한 보장**:
   - 칠판 알림 메모(`memos`), 숙제(`homeworks`), 숙제 제출 체크(`hw_checks`), 행동 관찰(`behavior_records`), 상담 일지(`consultations`)가 저장되는 `homeroom_classes/{document=**}` 경로는 담임 교사뿐만 아니라 관리자/시스템 설정 담당자도 저장 및 열람할 수 있도록 `firestore.rules`에 `allow read, write: if isSignedIn();` 규칙을 필수로 보장한다.

## Google Drive 중앙 저장소 학년별 수업자료 공유 체계 및 담임 관리소 연동 규칙

1. **표준 학년별 수업자료 공유 폴더 계층 구조 (1~6학년)**:
   - 학교 Google Drive 중앙 저장소의 각 학년도 폴더(예: `2026학년도`) 내에 `05_학년별 수업자료 공유` 폴더를 필수 생성하며, 그 하위에 `1학년`, `2학년`, `3학년`, `4학년`, `5학년`, `6학년` 6대 전용 폴더를 자동으로 동기화(`syncGradeMaterialsFolders`)한다.
   - 하위 폴더 ID 및 웹 링크(`gradeSubFolders`)는 Firestore의 `settings/googleDriveConfig` 내 `yearlyFolders[academicYear]`에 안전하게 영구 보존한다.

2. **담임 교사 업무 관리소 내 학년 자료 공유 탭 연동 및 직관적 파일 관리**:
   - 담임 교사 업무 관리소(`/teacher/homeroom`)의 학급 교실 관리(`ClassManagementTab`) 서브탭과 메인 탭 네비게이션 양쪽에 `[학년 자료 공유]`(`grade-materials`) 탭을 필수로 제공한다.
   - 접속 시 현재 선택된 담임 학급(`classKey`, 예: `4-4`)으로부터 학년(1~6)을 자동 추출하여 본인 학년 폴더를 최우선으로 바인딩하며, 상단 학년 전환 버튼을 통해 타 학년 자료도 즉시 탐색할 수 있도록 지원한다.
   - 해당 학년의 Google Drive 폴더로 즉시 이동할 수 있는 `[Google Drive에서 열기]` 원터치 버튼을 제공함과 동시에, KISAPP 화면 내에서 직접 파일을 드래그 앤 드롭으로 업로드(`POST /api/drive/upload-file`)하고 실시간 파일 목록(`GET /api/drive/list-files`)을 카드형으로 열람 및 다운로드할 수 있도록 보장한다.

## 학부모 상담 주간 예약 시스템 및 학부모 대시보드 스마트 알림 규칙

1. **Firestore runTransaction 기반 동시성 제어 및 학생 정보 자동 바인딩**:
   - 학부모 상담 주간 예약 시스템은 복수의 학부모가 동일 슬롯을 동시 클릭할 때의 이중 예약을 원천 차단하기 위해 Firestore `runTransaction`을 필수로 적용한다.
   - 학부모 전용 예약 화면(`/parents/consultation`)은 로그인 세션의 프로필(`studentName`, `studentGrade`, `studentClass`, `parentPhone`)을 자동 바인딩하여 별도의 학생 정보 입력 과정을 배제하고, 원터치 클릭만으로 즉시 예약을 확정하도록 구현한다.

2. **학부모 상담 유도 팝업 제어 및 대시보드 현행 신청 알림 카드 연동**:
   - 담임 교사 업무 관리소([ParentConsultationTab](file:///c:/myapp/kisapp/src/components/teacher/homeroom/ParentConsultationTab.tsx)) 상단에 명시적인 **[상담 접수 시작 / 상담 접수 종료]** 원터치 액션 버튼을 배치하여, 클릭 즉시 Firestore `isOpen` 플래그를 저장 및 동기화한다.
   - **신청 접수 기간(`isOpen: true`) 제어**:
     1) 담임 교사가 '상담 접수 시작'을 활성화했을 때만 학부모 로그인 시 상담 신청 유도 팝업(`ParentConsultationModal`)이 표출되며, 대시보드([page.tsx](file:///c:/myapp/kisapp/src/app/parents/page.tsx))에 "학부모 상담 주간 접수 중" 안내 및 신청하기 버튼이 나타난다.
     2) 담임 교사가 '상담 접수 종료'를 누르면 유도 팝업과 미신청 대상 안내 배너는 즉시 사라지며, 신청 페이지 진입 시 접수 마감 안내가 표시되고 신규 신청이 차단된다.
   - **상담 일정 확인창의 독립 유지 및 상담일 경과 시 자동 소멸**:
     1) 학부모가 이미 상담 예약을 완료한 경우, 담임이 상담 접수를 종료(`isOpen: false`)하더라도 학부모는 본인의 상담 일정을 대시보드 및 상세 페이지에서 반드시 확인할 수 있어야 한다. 따라서 확정된 일정 확인 카드(`showMyConsultationSchedule`)는 접수 종료 여부와 관계없이 상담일 당일까지 계속 유지된다.
     2) 학부모의 실제 상담 예약 날짜가 지나가면(`myConsultationSlot.date < todayStr`), 대시보드의 상담 일정 확인창은 화면 공간 최적화를 위해 자동으로 소멸(숨김) 처리된다.
   - **Google Calendar 원클릭 바로 등록 지원**:
     - 상담 예약을 완료한 학부모의 상세 일정 화면과 대시보드 확정 카드에 **[구글 캘린더에 등록]** 버튼을 필수 제공한다.
     - `.ics` 파일 다운로드/업로드 방식의 번거로움을 원천 배제하고, Google Calendar 웹 URL 파라미터(`action=TEMPLATE&text=...&dates=...&details=...&location=...`)를 자동 생성하여 클릭 1회로 구글 캘린더 일정 등록 창이 즉시 열리고 원터치 저장되도록 지원한다.
   - 이미 예약을 완료한 학부모에게는 유도 팝업을 노출하지 않으며, 미신청 학부모를 위해 "이번 주간 다시 보지 않기"(`localStorage`) 옵션을 필수로 제공한다.

3. **연계 교육 서비스(스쿨버스/방과후) 2열 컴팩트 위젯 및 상태 단일화**:
   - 학부모 대시보드 상단 메뉴바에 '신청서', '내역' 바로가기가 존재하므로, 대시보드 내 불필요하게 면적을 차지하는 대형 신청서/내역 카드는 완전 제거한다.
   - 출결/체험학습 신청서가 제출되어 결재 진행 중(`status === 'pending'`)일 때만 실시간 결재 상황 배너를 간결하게 노출하여 대시보드의 화면 공간을 최적화한다.
   - 연계 교육 서비스(스쿨버스/방과후학교)는 2열 그리드(`grid grid-cols-2`)로 나란히 50%씩 배치하여 모바일에서도 한눈에 파악되도록 슬림화한다. 장황한 안내문은 일절 배제하고 신청 시 `${bus.name} 탑승`, `${dayLabel}${title}` 등 핵심 현행 정보만 표출하며, 미신청 시 '미신청', '미수강'으로 단정하게 표기한다.
   - 모바일 환경에서 상단 서브 메뉴바(`신청서`, `내역`, `스쿨버스`, `방과후`)는 화면 스크롤 시에도 헤더 바로 아래에 안정적으로 고정되도록 `sticky top-[41px] sm:top-[64px] z-40 bg-card/95 backdrop-blur-md` 스타일을 적용한다.

## Google Drive 중앙 저장소 '06_체육 측정 결과' 폴더 체계 및 졸업사정회 아카이빙 연동 규칙

1. **표준 '06_체육 측정 결과' 폴더 동기화 및 영구 아카이빙**:
   - Google Drive 중앙 저장소의 각 학년도 폴더(예: `2026학년도`) 하위에 `06_체육 측정 결과` 폴더를 필수 동기화(`syncPeResultsFolder`, `peResultsId`, `peResultsUrl`)하여 관리자 설정 및 연말 아카이브 저장소로 자동 바인딩한다.

2. **다년간 시계열 성장 추적 DB(Firestore)와 연말 공식 엑셀 아카이브(Google Drive) 이원화 원칙**:
   - 학생의 4~6학년 3개년 체력 변화 추이, 현장 측정 시 이전 기록 즉시 대조 및 AI 성장 분석은 초고속 인덱싱 쿼리가 가능한 Firestore(`pe_schools/KISH/records`)에 영구 누적 보존한다.
   - 연말 측정이 완료된 시점에는 체육 기록 조회 화면(`RecordBrowser`)에서 `[Drive 아카이브]`를 통해 6학년 졸업 사정회 제출용 체력 평가 결과표(5대 요인별 등급, 20점 만점 종합 점수, 최종 1~5등급 완비) 및 전교생 종합 결과표를 엑셀 시트로 자동 빌드하여 Google Drive `06_체육 측정 결과` 폴더로 1터치 업로드(`peDriveArchiveService`)하고 학교 공식 문서고로 영구 보존한다.

## PAPS 학생 건강체력평가 개인별/학급별 통지표 인쇄 및 룰 기반 맞춤형 처방 표준

1. **AI(LLM) 의존성 원천 배제 및 사전 검증된 룰 기반 맞춤형 멘트 풀 매칭**:
   - 학생에게 배부되는 공식 건강체력평가 결과 통지표는 AI 호출 딜레이 및 환각(Hallucination) 위험을 차단하기 위해 사전 검증된 교육용 표준 멘트 풀(`papsReportCommentEngine.ts`)로 100% 구성한다.
   - **4대 맞춤형 멘트 체계**:
     1) **[영역 1] 종합 등급별 총평**: 교육부 종합 점수(20점 만점) 환산 결과 산출된 1~5등급에 따른 맞춤형 총평 멘트 자동 조합.
     2) **[영역 2] 최고 성취 역량 격려**: 5대 요인 중 등급이 가장 우수한 요인을 식별하여 학생의 성취감과 동기를 고취하는 칭찬 멘트 부여.
     3) **[영역 3-1] 우수 요인 심화 스포츠 추천 (1~2등급)**: 우수 요인의 지속적인 퍼포먼스 향상과 전문 활동 연계를 위해 학년별 신체 발달 수준(3~4학년: 킨볼, 넷볼, 볼더링 vs 5~6학년: 크로스핏 키즈, 스포츠클라이밍, 인터벌 러닝 등)에 맞춘 스포츠 종목 추천.
     4) **[영역 3-2] 취약 요인 놀이·게임형 처방 (4~5등급)**: 취약 요인에 대해 거부감 없는 동기 부여를 위해 일상 속 게이미피케이션 놀이(정글짐, 짐볼 볼링, 풍선 릴레이, 댄스 챌린지 등) 중심의 활동 처방.
     5) **[영역 4] 체질량지수(BMI) 및 생활 습관 가이드**: 과체중(가족 함께 걷기, 관절 부담 없는 수영, 계단 오르기) 및 저체중(규칙적 세끼 식사, 수면, 코어 근력 자극) 맞춤형 생활 가이드 제공.

## 교직원 캘린더 동기화 구글 캘린더 원클릭 바로 등록 표준

1. **파일 다운로드 없는 Google Calendar 원클릭 직접 등록 일원화**:
   - 기존의 번거로운 `.ics` 파일 다운로드/업로드 수동 방식 대신, 브라우저에서 Google Calendar Web URL 템플릿(`action=TEMPLATE&text=...&dates=...&details=...&location=...`)을 즉시 호출하는 원클릭 등록 방식을 교직원 시스템 전반에 표준 적용한다.
   - **적용 대상 및 연동 구조**:
     1) **빠른 도구 캘린더 동기화 (`AcademicCalendarSyncModal`)**: 모달 내 [구글 캘린더에 바로 등록] 액션 버튼 및 배정된 등교지도 근무일 목록/공식 학사 행사 각 항목별 `[+캘린더]`, `[캘린더 등록]` 원터치 링크 제공.
     2) **주요 학교 일정 (`ScheduleCalendarSyncModal` 및 대시보드 리스트)**: 대시보드 및 모달 내 [구글 캘린더 바로 열기] 버튼과 개별 학교/부서 일정 우측의 `[+캘린더]` 원터치 링크 제공.
     3) **나의 업무 (`inbox/page.tsx` 내 할당 업무)**: 헤더 [내 캘린더 동기화] 클릭 시 가장 임박한 마감 업무의 구글 캘린더 등록 창을 새 탭으로 즉시 열고, 개별 할당 업무 카드 우측에도 마감일이 있는 경우 `[+캘린더]` 링크를 제공하여 원터치 등록 지원.
   - 대량 일괄 캘린더 등록을 원하는 사용자를 위한 `.ics 다운로드` 기능은 백업 옵션으로 완벽히 보존한다.

## 담임 교사 모바일 뷰 최상단 헤더 배지 일원화 및 출석부 스크롤 고정 표준

1. **최상단 앱바 타이틀·권한 배지 일원화 및 본문 중복 배너 원천 배제**:
   - 모바일 화면의 담임 교사 업무 관리소(`/teacher/homeroom`)에서는 상단 공간 효율 극대화를 위해 본문 내부의 중복 타이틀 배너를 완전히 제거하고, 최상단 앱바(`MainLayout`의 `title` prop)에 페이지명(`담임 업무`)과 권한 배지(`[전교 권한]` 또는 `[학급 담임]`)를 인라인으로 일원화 표출한다.

2. **물리적 Flexbox 고정(`shrink-0`) 및 뷰포트 격리를 통한 덜컹거림 원천 차단**:
   - 브라우저 스크롤 이벤트에 따라 위치를 재계산하는 CSS `sticky`에 의존할 경우, 모바일 터치 스크롤 시 상단 조작부 터치로 인한 오작동 및 미세한 덜컹거림(Jittering)이 발생할 수 있다.
   - 따라서 `(app)/layout.tsx`에서 담임 업무 페이지 접근 시 모바일 메인 컨테이너에 `overflow-hidden pb-0`을 적용하여 전체 페이지 스크롤을 원천 잠금한다.
   - `MainLayout`에는 `isFixedScreen={true}`를 적용하고, 5대 탭 버튼과 출석부 조작 헤더는 `shrink-0` flex item으로 물리적으로 화면 상단에 100% 고정 배치한다.
   - 상단 고정 영역을 터치하거나 문질러도 스크롤 이벤트가 절대 발생하지 않으며, 덜컹거림 없이 완전하게 물리적으로 고정되도록 보장한다.

3. **출석부 카드 내부 중복 헤더 제거 및 학생 명단 단독 스크롤 격리**:
   - 출석부 탭 콘텐츠(`TabsContent value="student-info"`) 내부에는 중복된 `CardHeader`를 배제하고, 카드 내부의 학생 명단 컨테이너(`CardContent`)에만 `flex-1 min-h-0 overflow-y-auto`를 부여하여 **오직 학생 명단 리스트만 단독으로 부드럽게 스크롤**되도록 구현한다.
   - 대리작성, 학급관리 등 타 탭 전환 시에는 출석부 헤더가 자동으로 숨겨지고 5대 탭만 고정된 채 각 탭별 고유 폼과 컨텐츠가 독립 스크롤되도록 격리한다.

4. **출결/체험학습 대리작성 상단 컨트롤 일체형 고정 및 초슬림 폼 표준**:
   - 대리작성(`proxy`) 탭에서는 **학급 선택, 학생 선택, 결재일자(신청일자) 3개 버튼**을 단일 행(`grid grid-cols-3 gap-1.5`)으로 배치하고, 그 아래의 **서식 선택 탭(`교외체험학습 신청서` / `결석계`)**까지를 상단 고정 영역(`shrink-0`)으로 묶어 배치하여 화면 스크롤 시에도 뷰포트 상단에 완전히 물리적으로 고정한다.
   - 교외체험학습 신청서: 시작일/종료일 나란히 2분할(`grid grid-cols-2`), **학습 형태, 방문 장소, 동행 보호자, 학생과의 관계 4개 입력 필드**를 단일 행(`grid grid-cols-4 gap-1 sm:gap-2`)에 컴팩트하게 배치한다.
   - 결석계: 시작일/종료일 나란히 2분할(`grid grid-cols-2`), **결석 종류와 담임 확인 방법**을 단일 행(`grid grid-cols-2 gap-1.5 sm:gap-2`)에 나란히 배치한다.
   - 오직 그 아래의 실제 신청서 입력 폼 본문(`CardContent`)만 `flex-1 min-h-0 overflow-y-auto`로 내부 독립 스크롤되도록 격리한다.

## 학교 체육(PE) 모바일 뷰포트 헤더/툴바 1줄 컴팩트화 및 스크롤 고정 표준

1. **상단 컨트롤 바 및 필터 툴바 1줄 컴팩트화 (`flex-nowrap`)**:
   - 모바일 뷰(`/teacher/pe`)에서 1단계 대메뉴('측정 & 분석'), 2단계 소메뉴('기록 입력'), 담당 설정, AI 인텔리전스, 새로고침 및 접기 액션 버튼은 줄바꿈 없이 단 1줄(`flex-nowrap overflow-x-auto no-scrollbar`)에 컴팩트하게 배치한다.
   - 드롭다운 트리거(`SelectTrigger`)는 `[&_svg]:hidden sm:[&_svg]:block`을 적용하여 모바일에서 아래 꺾쇠 아이콘을 숨기고, 텍스트 라벨을 한 글자/약어로 축약하여 버튼의 좌우 폭을 최소화한다.
   - 툴바 1행은 모드 스위치 탭(`학급/팀별`, `개별`)과 진행률 요약(`진행: 0/204명 (0%)`)을 `flex justify-between w-full flex-nowrap` 단 1줄로 통합한다.
   - 툴바 2행은 학년, 반, 그룹, 날짜, 종목, 템플릿 다운로드, 전체 저장(디스켓 `<Save>` 아이콘), 접기 버튼을 단 1줄(`flex-nowrap overflow-x-auto no-scrollbar`)로 정렬한다. 종목명은 최대 5자(`batchRecordItem.slice(0, 5)`)로 제한하고, 날짜 버튼의 달력 아이콘을 제거한다.

2. **명단 스크롤 시 2대 컨트롤 영역 상단 고정 (`sticky`) 및 내부 스크롤 격리**:
   - 상단 헤더 컨트롤 바는 `sticky top-[48px] sm:top-0 z-30 bg-background/95 backdrop-blur-md`로 고정한다.
   - 기록 입력 툴바 컨테이너는 헤더 바로 아래인 `sticky top-[118px] sm:top-[50px] z-20 bg-background/95 backdrop-blur-md`로 고정한다.
   - 학생 명단 테이블 컨테이너는 `max-h-[calc(100dvh-265px)]` 높이와 `overflow-y-auto`를 적용하여 내부 독립 스크롤을 보장하며, 테이블 헤더(`thead`)는 `sticky top-0 bg-slate-100 z-10`과 불투명 배경을 적용하여 스크롤 시 행 내용이 헤더 뒤로 비치지 않도록 격리한다.

3. **성장 분석(`ClassAnalytics`) 모바일 1줄 필터 및 순수 테이블 스크롤 격리 표준**:
   - 학생 이름 검색창(`placeholder="학생 이름"`, `w-[78px]`), 검색 버튼(돋보기 아이콘 전용), 학년(`w-[48px]`), 반(`w-[40px]`), 클럽(`placeholder="클럽"`, `w-[56px]`), 초기화 버튼을 단 1줄(`flex-nowrap overflow-x-auto no-scrollbar`)로 컴팩트하게 배치하고, 드롭다운 트리거의 아래 꺾쇠 아이콘을 숨김 처리(`[&_svg]:hidden sm:[&_svg]:block`)한다.
   - 검색/필터 바 컨테이너에 `sticky top-[118px] sm:top-[50px] z-20 bg-background/95 backdrop-blur-md`를 적용하여 상단 컨트롤 헤더 바로 아래에 밀착 고정한다.
   - 컴포넌트 라이브러리의 `Table` 래퍼 대신 순수 HTML `table`, `thead`, `tr`, `th`, `tbody`, `td` 구조를 사용하고, 테이블 컨테이너에 `max-h-[calc(100dvh-270px)] sm:max-h-[calc(100dvh-220px)] overflow-y-auto scrollbar-thin`을 적용하며, `thead`에 `sticky top-0 z-10 bg-slate-100 shadow-xs`를 부여하여 명단 스크롤 시에도 헤더가 상단에 착 붙어 유지되도록 격리한다.
   - 학생 검색어(`searchTerm`) 입력 시 `filteredStudents` 메모에서 이름 포함 검색 결과를 즉시 반환하여 검색 대상 목록이 테이블에 바로 표출되도록 지원한다.

4. **기록 조회(`RecordBrowser`) 모바일 1줄 필터 및 테이블 가로 슬라이드 방지 표준**:
   - `PAPS 종합` / `종목별 기록` 탭 바 우측에 `통지표 인쇄`, `엑셀 다운로드`, `Google Drive 저장` 액션 버튼 3종을 인라인(`shrink-0`)으로 통합 배치한다.
   - 필터 바: PAPS 종합(`클럽`, `학년`, `반`, `달력 아이콘만`, `등급`) 및 종목별 기록(`종목(4자 제한)`, `클럽`, `학년`, `반`, `달력 아이콘만`)의 5개 컨트롤을 단 1줄(`flex items-center gap-1 sm:gap-1.5 overflow-hidden w-full`)에 컴팩트하게 배치한다. 드롭다운 트리거의 아래 꺾쇠 아이콘은 모바일에서 숨김 처리(`[&>svg]:hidden sm:[&>svg]:block`)하고, 날짜 필터는 텍스트 없이 달력 아이콘만 노출한다.
   - 테이블: `학년`, `반` 열을 완전 배제하고 `번호`부터 시작하도록 구성한다. `Table className="w-full table-fixed"` 및 부모 컨테이너 `overflow-hidden`을 적용하고, 모바일 헤더 축약(`getDisplayHeader`: 심폐, 근력, BMI, 등급 등 2~3글자) 및 셀 값 4글자 제한(`getDisplayCellValue`)을 적용하여 390px 화면에서도 좌우 슬라이드(가로 스크롤)가 일절 발생하지 않도록 100% 뷰포트에 수납한다.

5. **토너먼트/리그(`TournamentManagement`) 모바일 1줄 상단 툴바 및 생성 폼 모달 격리 표준**:
   - 상단 컨트롤 툴바: 대회 선택 드롭다운(`SelectTrigger w-[140px] [&>svg]:hidden sm:[&>svg]:block`), 새 대회 생성 버튼(`[+ 생성]`), 대회 삭제 버튼, [대회 선택 시] `경기목록`/`대진표` 탭 스위치, 팀 이름 수정(아이콘화 `<Pencil>`), 대진표 인쇄(아이콘화 `<Printer>`) 액션 전체를 단 1줄(`shrink-0 w-full overflow-hidden flex items-center justify-between`)에 컴팩트하게 배치한다.
   - 대회 생성 폼 모달 분리: 화면의 상당 부분을 차지하며 긴 세로 스크롤을 유발하던 인라인 폼을 전용 `Dialog open={isCreateDialogOpen}` 팝업 모달로 완전 격리하여, 메인 화면의 군더더기 없는 뷰포트를 확보한다.
   - 경기 카드 및 대진표 독립 스크롤: 본문 영역 컨테이너에 `flex-1 min-h-0 overflow-y-auto overscroll-contain`을 부여하여, 외부 부모 윈도우 스크롤 및 덜컹거림(Jittering)을 원천 차단하고 경기 카드 그리드(`Card px-2.5 py-1 text-xs`, 팀 점수 `w-12 h-6 font-black`) 내에서만 부드럽게 스크롤되도록 단일화한다.

6. **팀 밸런서(`TeamBalancer`) 모바일 1화면(Zero-Scroll) 2열 스플릿 뷰 및 모달 격리 표준**:
   - 상단 통합 컴팩트 툴바: 편성 로드 드롭다운(`w-[105px]`), `[새 편성]`, `[삭제]`, 편성 이름 입력창, `[저장/전송]` 버튼을 단 1줄(`px-2.5 py-1.5 shrink-0 flex items-center justify-between gap-1.5`)에 배치한다.
   - 세그먼트 탭 헤더: `[팀 배정 (N팀)]`, `[조건 설정 (N명)]`, `[학생 분석 (N명)]` 3대 탭과 우측 퀵 `[자동 편성]` 액션 버튼을 상단에 고정(`shrink-0`)한다.
   - 2열 컴팩트 스플릿 뷰: 팀 배정(`teams`) 화면은 `grid grid-cols-2 gap-1 sm:gap-2 flex-1 min-h-0` 2열 분할을 적용하여, 좌우로 팀별 학생 목록 및 점수가 한눈에 들어오고 외부 페이지 스크롤 없이 실시간 비교 및 조작이 가능하도록 수납한다.
   - 팀 멤버 이동 모달 격리: 학생 행 터치 시 상위 페이지가 스크롤되거나 레이아웃이 밀리지 않도록 전용 `Dialog` 팝업 모달을 띄워 이동 대상 팀을 원터치로 선택하고, 팀 간 학생 이동 및 평균 점수/인원수 재계산이 즉각 양방향 반영되도록 보장한다.
   - 조건 설정 원터치 칩: 아코디언 대신 학년/반 칩, PAPS/전체선택 칩, 성별 4분할 버튼, 밸런스 로직(균등/실력순/랜덤) 버튼 그룹을 직관적인 칩 형태로 배치하여 스크롤 피로도를 원천 차단한다.

7. **체육 이론평가(`TheoryExamManagement`) AI 생성기 Zero-Scroll 3탭 모바일 UX 표준**:
   - 모바일 화면(`lg:hidden`)에서는 출제 설정, 문제지 검토/편집, 보관함 및 배포 현황을 상하로 길게 나열하지 않고, 상단 3탭 세그먼트(`[문제 출제]`, `[문제지 확인 (문항수)]`, `[보관함/현황 (저장/배포)]`)로 분리하여 뷰포트 100% Zero-Scroll을 보장한다.
   - 텍스트 입력창은 `flex-1 min-h-[100px]`로 남은 뷰포트 공간을 유연하게 채우고, 5문항 선택과 AI 생성 버튼, 유튜브 URL 입력창은 한 줄에 밀착 배치한다.
   - 파일 첨부 버튼은 브라우저 기본 `<input type="file">`의 들쭉날쭉한 UI 대신 숨김 `ref` 패턴과 컴팩트 버튼(`h-7 text-xs`)으로 구현한다.
   - AI 문제 생성 완료 시 또는 보관함에서 퀴즈를 불러올 때는 즉시 `mobileTab = 'quiz'`(문제지 확인 탭)로 자동 전환하여 작업 흐름의 단절을 방지한다.
   - 데스크톱 환경(`hidden lg:flex`)에서는 좌측 설정/보관함 패널(380px)과 우측 문제지 뷰어 2패널 고정 레이아웃을 제공한다.

## 모바일 뷰포트 부모 컨테이너 덜컹거림(Jittering/Bounce) 원천 차단 전역 표준

1. **전역 레벨 오버스크롤 바운스 차단 (`globals.css`)**:
   - `html, body`에 `overscroll-behavior-y: none; -webkit-overflow-scrolling: touch;`를 전역 적용하여 모바일 브라우저의 전형적인 고무줄 바운스(Rubber-banding) 및 상위 뷰포트 흔들림을 원천 봉쇄한다.

2. **루트 앱 프레임의 물리적 뷰포트 고정 (`AppLayout`)**:
   - `(app)/layout.tsx`에서 특정 페이지(예: `isHomeroom`)를 하드코딩 검사하여 땜질 처리하는 방식을 일절 금지한다.
   - `AppLayout`의 `main` 및 직속 `div`는 모바일/데스크톱 구분 없이 상시 `overflow-hidden h-full min-h-0 flex flex-col pb-0 overscroll-none`으로 고정하여, 부모 컨테이너가 독자적으로 스크롤을 발생시키거나 덜컹거리는 현상을 100% 원천 차단한다.

3. **스크롤 책임의 단일화 (`MainLayout`)**:
   - `MainLayout`은 `h-full max-h-full min-h-0 overflow-hidden overscroll-none` 프레임으로 뷰포트에 밀착 배치하며, 상단 `header`는 `shrink-0 z-30`으로 물리적 고정한다.
   - `isFixedScreen={true}` 모드(체육, 담임, 스쿨버스 등 내부 목록 스크롤을 가진 작업 화면): `main`을 `overflow-hidden flex flex-col min-h-0 pb-16 lg:pb-0`으로 설정하여 상단 컨트롤 요소는 `shrink-0`으로 고정하고, 자식 목록 영역만 `flex-1 min-h-0 overflow-y-auto overscroll-contain`으로 부드럽게 스크롤되도록 격리한다.
   - `isFixedScreen={false}` 모드(일반 문서/조회 화면): `main` 컨테이너에 `overflow-y-auto overscroll-contain pb-20 lg:pb-0`을 단일 적용하여 본문 전체가 하나의 독립 스크롤 컨텍스트에서만 스크롤되도록 단일화한다.
   - 화면 전체에서 스크롤 가능한 요소는 오직 1개만 존재해야 하며, 부모와 자식이 이중으로 `overflow-y-auto`를 가짐으로써 발생하는 스크롤 경합(Scroll Chaining) 및 덜컹거림을 원천 금지한다.

