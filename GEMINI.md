# Workspace Rules (KISAPP)

## 브라우저 테스트 및 MCP 제어 규칙
- **세션 초기화 금지**: 새 브라우저 컨텍스트를 임의로 재시작하거나 첫 페이지로 리셋하지 말 것.
- **현재 열린 세션 유지**: 이미 열려 있는 탭/페이지가 있다면 새로고침이나 새 창 열기 없이 해당 화면 상태(URL, 로그인 세션)에서 작업을 이어갈 것.
- **단계별 확인**: 화면 상태가 예상과 다르면 임의로 다른 링크를 누르지 말고, 현재 화면 스크린샷이나 에러 상태를 보고하고 대기할 것.

## Firebase 프로젝트 격리 및 배포 안전 규칙
1. **프로젝트 ID 바인딩 확인**:
   - Firebase 관련 작업(보안 규칙 배포, Functions, Firestore 조회 등)을 수행할 때는 절대 MCP의 기본 세션 값에 의존하지 않는다.
   - 작업 시작 전 반드시 현재 워크스페이스의 `.firebaserc` 또는 `firebase.json`에서 정확한 `projectId`를 확인하고, `firebase_update_environment`를 통해 현재 프로젝트 디렉토리와 `active_project`를 1:1로 일치시킨다.
2. **배포 시 명시적 프로젝트 플래그 필수**:
   - CLI 명령어로 Firebase 배포(`deploy`)를 실행할 때는 반드시 `--project <프로젝트ID>` 플래그를 명시하여 타 프로젝트로의 오배포를 원천 차단한다.
3. **배포 승인 규칙**:
   - 사용자의 명시적인 배포 승인("배포해", "배포해줘")이 있을 때만 원격 푸시 및 배포 명령을 실행한다.

## 스쿨버스 및 방과후 도메인 로직 규칙
1. **경로 및 요일 엄격 필터링**:
   - 스쿨버스 목적지 파악, 미편성 목적지 계산, 탑승 학생 필터링 시 반드시 선택된 **경로(`routeType`: 등교/하교/방과후)**와 **요일(`dayOfWeek`)**에 실제로 탑승하는 학생만 대상으로 추출한다.
   - 특히 `AfterSchool`(방과후)의 경우, 해당 요일에 방과후 수업을 실제로 수강하거나 방과후 목적지가 등록된 학생만 대상으로 해야 하며, 수업이 없는 요일(금요일 등)에는 대상자가 0명으로 정확히 처리되어야 한다. 일반 하교 목적지(`afternoonDestinationId`)를 임의 fallback으로 사용하여 대량의 오탐 목적지가 발생하는 것을 원천 차단한다.
   - 해당 경로/요일의 노선(stops)에 이미 등록되어 있는 목적지(ID 및 명칭 매칭)는 미편성 목록에서 완벽하게 제외한다.

2. **관계형 참조 필드 유효 데이터 검증 (Orphan ID 누출 방지)**:
   - 버스 번호, 목적지 등 참조 필드를 렌더링할 때는 유효성 검증 없이 Firestore 문서 ID를 fallback으로 화면에 노출하지 않는다.
   - 반드시 실제로 등록된 유효 데이터(`b.name` 존재)만 필터링하고 번호순으로 정렬하여 사용자 화면에 유령 ID가 노출되는 것을 원천 차단한다.

3. **버스 배정 제외(Excluded) 설정의 경로 타입별 격리**:
   - 등하교(`commute`), 평일 방과후(`afterSchool`), 토요일(`saturday`) 각 노선의 배정 제외 상태는 단일 boolean 플래그가 아닌 노선 타입별 독립 필드(`excludeFromAssignmentByType`)로 분리 저장 및 조회한다.
   - 특정 노선(예: 방과후)에서 배정 제외된 버스가 다른 노선(예: 등하교)의 배정 대상이나 검색 목록에서 누락되는 간섭을 원천 차단한다.

4. **방과후 수강 명단과 스쿨버스 명단 간 동명이인 오매칭 방지**:
   - 방과후 수강생(`enrollments`)과 스쿨버스 학생(`students`) 간 상호 대조 및 자동 배정 시, 이름 단독 fallback 매칭을 절대 금지한다.
   - 반드시 고유 `studentId` 우선 매칭을 수행하고, 식별자가 없는 경우 `이름 + 학년 + 반`(동일 학년/반 엄격 일치) 복합 키로만 매칭하여 타 학년/반 동명이인으로의 오배정을 원천 차단한다.

5. **배정 대상 버스 목록의 실운행 필터링 필수**:
   - 교사 및 학생 배정 테이블/선택 모달에서는 전체 버스를 무조건 나열하지 않고, 선택된 노선/요일에 실제로 정류장(`stops`)과 운행 계획이 존재하는 실운행 버스(`isBusOperational`)만 필터링하여 노출한다.

6. **모달/팝업 스크롤 바운스 방지 (`overscroll-contain`)**:
   - 교사/학생 선택 모달 등 내부 스크롤 목록 컨테이너에는 반드시 `overscroll-contain` 스타일을 적용하여, 끝단 스크롤 시 부모 윈도우 스크롤 연쇄로 인한 화면 떨림(Jittering)을 원천 차단한다.

7. **수강 취소(`CANCELLED`) 및 비확정 신청 배제와 학기/방학 모드 격리**:
   - 스쿨버스 시스템에서 방과후 수강 명단(`afterschool_enrollments`)을 학생 데이터에 연동하거나 학생 카드에 렌더링할 때, 취소된 신청(`status === 'CANCELLED'`)이나 미확정 상태는 반드시 원천 배제하고 오직 확정(`ENROLLED`) 상태만 매핑한다.
   - 학기중 모드(`regular`)와 방학 모드(`vacation`)는 강좌의 실제 학기 속성(`semesterMode`, `semester`, `period`, `title`)에 따라 완벽히 분리 처리하며, 학기중 모드에서는 방학 강좌 수강 내역이 학생 카드나 목적지에 절대 혼선 및 간섭되지 않도록 격리한다.

8. **스쿨버스 학생 카드 내 방과후 수업 제외(X) 시 양방향 완결 동기화**:
   - 스쿨버스 관리자에서 학생의 방과후 수업 명단 제외(X)를 실행할 경우, `afterSchoolClassIds`뿐만 아니라 학생 객체의 `enrolledCourseTitles`, `afterSchoolCourseTitles`, `afterSchoolCourseTitle` 및 UI 로컬 state를 즉시 갱신해야 한다.
   - 동시에 메인 방과후 수강 컬렉션(`afterschool_enrollments`)의 해당 신청 문서도 `status: 'CANCELLED'`로 동기화하여 실시간 리스너에 의한 재등장이나 방과후 관리자 페이지와의 불일치를 원천 차단한다.

9. **학생 카드 조회 시 탑승 버스 좌석표 연동 및 미배정 조건부 렌더링**:
   - 탑승 학생 관리에서 미배정 목록 클릭 또는 전체 학생 검색으로 특정 학생을 조회할 때, 해당 학생이 현재 선택된 요일/경로의 버스 노선에 배정되어 있다면 학생 카드 내에 탑승 버스명, 좌석 번호 뱃지 및 인라인 버스 좌석표(`BusSeatMap`)를 함께 렌더링하고 해당 좌석을 시각적으로 강조한다.
   - 학생이 해당 요일/경로에 미배정 상태인 경우에는 좌석표를 렌더링하지 않고 학생 기본 카드만 간결하게 표시한다.
   - 상단 목적지 오류(배정 불가) 카드는 화면 공간을 과도하게 점유하지 않도록 기본 접힘(`isUnassignableFolded = true`) 상태로 제공한다.

10. **방과후 강좌 강사진 상호 보완 원칙 (주강사/보조/추가강사 1~4 누락 방지)**:
    - 스쿨버스 관리자의 방과후 하교 버스 조회(`AfterSchoolManagementTab`) 및 교사용 방과후 명단 조회(`AfterSchoolInquiryDialog`)에서 강좌 정보를 표출할 때, 스쿨버스 DB(`after_school_classes`) 단독 데이터에 의존하지 않는다.
    - 메인 방과후 DB(`afterschool_courses`)의 강좌 정보를 실시간 구독하여 두 데이터 소스를 상호 보완(`getComplementaryInstructors`)하며, 주강사뿐만 아니라 추가강사(1~4) 및 보조강사(`assistantTeachers`, `assistantInstructor`) 전체를 식별하여 수업 선택 드롭다운, 검색 필터, 선택된 강좌 상세 카드 상단에 명확한 뱃지 및 전체 강사 명단으로 표출한다.

11. **탑승 명단 및 학생 카드 내 방과후 강좌 요일별 엄격 일치 (타 요일 강좌 노출 금지)**:
    - 스쿨버스 교사용 페이지(`/teacher/bus`) 등 버스 탑승 학생 명단 및 학생 카드에서 방과후 강좌명/뱃지를 표출할 때, 학생이 수강하는 전체 요일 강좌명(`afterSchoolCourseTitle`, `enrolledCourseTitles`)을 무조건적인 fallback으로 노출하지 않는다.
    - 반드시 현재 선택된 요일(`selectedDay`)에 실제로 수업이 진행되는 강좌(`afterSchoolCoursesByDay[selectedDay]`)만 대조하여 표출하고, 해당 요일에 방과후 수업이 없는 경우에는 뱃지 및 상세 카드의 방과후 항목을 일절 노출하지 않아 일반 하교 버스 탑승 시의 혼선을 원천 차단한다.

12. **로그인 교사 맞춤형 기본 선택 우선순위 (담당 버스 및 본인 방과후 강좌 우선 선택)**:
    - 스쿨버스 탑승 관리(`/teacher/bus`) 진입 시, 비동기 데이터 로딩 중 성급하게 '전체(`all`)'로 초기화하지 않으며, 로그인한 교사의 담당 버스(등하교/방과후/토요)가 확인되는 즉시 해당 버스를 1순위로 자동 선택한다. 담당 버스가 아예 없는 교사인 경우에만 전체(`all`)를 표시한다.
    - 방과후 수업 관리(`/teacher/afterschool`) 진입 시, 관리자 권한(`isAdmin`)이 있더라도 가나다 순 첫 번째 강좌가 아닌 로그인한 교사의 본인 담당 강좌(`myOwnCourses`: 주강사/보조/추가강사) 출석부를 최우선으로 기본 선택하고, 드롭다운 목록에서도 본인 강좌를 최상단에 배치하여 대리 출석 관리 편의성과 본인 수업 출석 확인의 직관성을 보장한다.

13. **하교 미배정 명단 내 방과후 이동 학생 식별 배지 ('종료후복귀') 표준화**:
    - 방과후 수업을 수강하여 방과후 버스 노선으로 이동한 학생은 방과후 종료 후 원래 하교 버스로 복귀해야 하므로 정규 하교 버스 좌석에서는 배제되어 하교 미배정 명단에 머무르게 된다.
    - 관리자가 하교 버스 좌석 배정 작업 중 이 학생들을 미배정으로 오인하여 하교 버스에 중복/재배정하는 혼선을 원천 차단하기 위해, 하교(`routeType: 'Afternoon'`) 미배정 카드에서는 해당 요일에 방과후 수업/목적지가 있는 학생에게 방과후 강좌명 배지 옆에 `'종료후복귀'`(`amber` 톤 배지)를 필수 노출한다.
    - 관리자 버스 페이지(`/admin/bus`)에서도 학생 데이터 병합 시 요일별 방과후 강좌(`afterSchoolCoursesByDay`)를 구축하여 타 요일 강좌 간섭 없이 해당 요일의 방과후 정보와 복귀 배지가 정확히 매핑되도록 보장한다.

## 학부모 신청서 및 공문서 출력/결재 서식 규칙
1. **결재란 직책 슬롯 표준화**:
   - 상단 결재란의 헤더 직책명은 결재자의 세부 보직명(`교무부장`, `학년부장`, `1반 담임` 등)을 직접 노출하지 않고, 반드시 표준 슬롯명(`['담임', '부장', '교감', '교장']`) 2글자 고정으로 렌더링하여 결재란 너비 붕괴 및 텍스트 쏠림을 방지한다.

2. **원본대조필 직인 노출 격리 (학부모 사본 전용)**:
   - 교원/관리자 결재문서함(`/documents/[id]`)은 학교 보관용 원본 문서이므로 하단 여백의 '원본대조필' 직인을 절대 표시하지 않는다.
   - 원본대조필 직인은 오직 학부모 포털(`/parents/documents/[id]`, 인쇄 및 PDF 다운로드)에서 결재가 완료(`approved`)된 문서에 한해 하단 우측 여백에 날인한다.

3. **A4 1페이지 규격 및 인라인 테두리 보존**:
   - 공문서 및 신청서 양식은 브라우저 인쇄와 PDF 변환 시 테두리가 유실되지 않도록 모든 `table`, `th`, `td`에 인라인 스타일(`border: '1px solid #000000'`, `borderCollapse: 'collapse'`)을 명시한다.
   - 1페이지 독립 서식(서식 1 신청서, 서식 2 결과보고서, 서식 3 결석계)은 A4 규격(297mm) 내에 완벽히 수납되도록 행 높이와 여백을 정밀 계산하여 하단 서약란 및 직인이 다음 페이지로 밀리는 것을 원천 차단한다.

4. **PDF 변환 엔진 및 화면 1:1 캡처 보존**:
   - A4 서식의 PDF 다운로드 변환 시 가상 뷰포트 재렌더링으로 왜곡을 유발하는 `html2canvas` 대신, 브라우저의 실제 렌더링 픽셀을 그대로 캡처하는 `html-to-image`(`toPng`, `pixelRatio: 2.5`, `cacheBust: true`, `width: el.offsetWidth, height: el.offsetHeight`)를 사용한다.
   - 사용자가 화면에서 보고 있는 렌더링 결과와 1픽셀의 오차도 없이 1:1로 일치하도록 보장한다.

5. **서명란 및 인명/직함 라벨 줄바꿈 원천 차단 (`whitespace-nowrap`)**:
   - 서약 및 서명 영역의 '보호자 :', '학부모 :', '학생 :' 등 라벨과 기안자/보호자명, 직인 슬롯은 폭 축소 시 콜론(`:`)이나 텍스트가 다음 줄로 떨어지지 않도록 반드시 `whitespace-nowrap` 및 인라인 플렉스 컨테이너(`inline-flex items-center whitespace-nowrap`)로 묶어 렌더링한다.

6. **테이블 셀 수직 중앙 정렬 및 2mm 여백 표준화**:
   - 모든 단일행 테이블 셀(`th`, `td`)은 텍스트가 셀 하단에 붙지 않도록 `display: flex; align-items: center;` 컨테이너 및 `padding: '2mm'`(또는 내부 `min-h` 및 `leading-tight`)를 적용하여 완벽한 수직 중앙 정렬과 시각적 여백을 유지한다.

## 통합 학생 마스터 및 대용량 데이터 로딩 규칙
1. **온디맨드 그룹(학년) 쿼리 필수 (초기 대량 패치 원천 차단)**:
   - 통합 학생 계정 관리(`/admin/students`) 등 수천 건 이상의 문서를 다루는 대용량 테이블에서는 초기 진입 시 전교생 컬렉션을 일괄 구독(`onSnapshot`)하지 않는다.
   - 반드시 사용자가 조회할 학년(`selectedGrade`)을 선택했을 때 해당 그룹에 한정하여 `where('grade', 'in', gradeValues)` 조건부 쿼리를 실행하며, 초기 상태는 '선택 안 함' 상태로 두고 직관적인 빈 상태 안내 UI와 바로가기 버튼을 제공하여 초기 로딩 지연과 불필요한 네트워크 트래픽을 원천 차단한다.

## 데스크톱 반응형 뷰포트 및 고정 푸터 레이아웃 규칙
1. **넓은 화면(데스크톱) 하단 푸터 고정 및 모바일 스크롤 격리**:
   - `AppLayout`에서 데스크톱(`lg:`) 환경은 최하단 푸터(`AppFooter`)를 `shrink-0`으로 화면 바닥에 고정하고, 본문 컨테이너는 `flex-1 min-h-0 overflow-y-auto`로 구성하여 푸터 위 남은 뷰포트 공간을 100% 수납한다.
   - 모바일 환경은 기존대로 `main` 컨테이너의 `overflow-y-auto pb-20`을 유지하여 자연스러운 전체 세로 스크롤 끝에 푸터가 노출되도록 격리한다.

2. **대시보드 비율형 뷰포트 맞춤 (Zero-Scroll 유지 및 고정 min-h 배제)**:
   - 전자결재 대시보드(`/inbox`) 등 한눈에 전체 현황을 파악해야 하는 메인 대시보드 화면에서는 카드 내부 컨테이너에 고정된 픽셀 높이(`min-h-[220px]` 등)를 강제하지 않는다.
   - 모든 수직 패널은 `flex-1 min-h-0`과 `overflow-y-auto scrollbar-thin`을 적용하여 화면 크기(노트북 768p부터 고해상도 모니터까지)에 비례하여 남은 공간을 유연하게 채우고, 내부 항목이 넘칠 때만 해당 카드 안에서 스크롤되도록 하여 브라우저 전체 창에 불필요한 스크롤바가 발생하는 것을 원천 차단한다.

## 학교 조직도 및 전자결재 수기 결재선 연동 규칙
1. **학교 리더십 수기 결재자(학교장/행정실장 등) 성명 직접 입력 및 공문서 결재선 자동 바인딩**:
   - 학교장(교장), 행정실장 등 전산 계정이 없어 수기 결재를 진행해야 하는 직책의 경우, 시스템 설정(조직도 > 학교 리더십)에서 계정 매핑이 없더라도 성명을 직접 입력(`principalName`, `administrativeHeadName` 등)하여 저장할 수 있도록 지원한다.
   - 공문서 작성기(`document-form`) 및 복무/초과근무 신청기(`teacher/duty`, `teacher/overtime`)는 조직도 설정에 성명이 등록되어 있는 경우 가상 프로필을 지원하고 결재선 생성 시 해당 성명을 `approver.name`으로 즉시 바인딩하여 결재란에 수기 결재자의 성명이 누락 없이 정확하게 인쇄/표출되도록 보장한다.
   - 3번째 결재 슬롯은 '협조' 외에 '행정실장'을 직책으로 선택할 수 있도록 지원하고, 선택 시 행정실장의 등록 성명이 결재선에 즉각 동기화되도록 한다.

## 반복 루틴 업무 자동화 및 부장 그룹 일괄 할당 규칙
1. **루틴(반복) 업무 주기 자동 산출 및 대시보드 리마인드 동기화**:
   - 주간/월간 단위로 정기 반복되는 업무(안전 점검표, 감염병 현황, 부장회의록 등)는 '루틴 업무 요청(`routine`)' 카테고리로 등록하며, `routineConfig`를 통해 반복 주기(`weekly`/`monthly`), 기준 요일/일자, 소요기간을 저장한다.
   - 전자결재 대시보드(`/inbox`)는 현재 기준일이 해당 루틴 업무의 활성 주기에 해당하고 본인이 미제출 상태인 경우, '나의 업무' 카드 상단에 루틴 알림 배너를 즉각 표출하여 주초/월초에 잊지 않고 신속하게 제출할 수 있도록 리마인드한다.
   - 업무 목록 및 제출 현황 모달 등 모든 뷰에서는 일반 업무와 구별되도록 '루틴 (주간/월간)' 배지를 필수로 노출한다.

2. **부장 그룹(`head`) 대상 자동 통합 추출**:
   - 부장 회의록, 부장단 공지 등 학교 리더십/중간관리자 전체를 대상으로 하는 업무 요청 시 '부장 그룹' 버튼을 제공한다.
   - 부장 그룹 선택 시 별도의 개별 선택 없이도 학교 조직도(`orgData`) 상의 행정 부서 부장(`departments.headEmail`), 각 학년 부장(`gradeHeads`), 교무부장(`academicHead`)을 자동으로 중복 없이 단일 대상군으로 추출하여 1회 클릭으로 정확하게 일괄 할당한다.

## 방과후학교 출석부 모바일 사용성 및 레이아웃 규칙
1. **모바일 뷰포트 본문 세로 스크롤 허용 (`overflow-y-auto`)**:
   - `MainLayout`의 `<main>` 컨테이너는 데스크톱 고정 뷰포트 최적화 시에도 모바일 화면에서 자식 요소가 길어졌을 때 아래로 자연스럽게 스크롤될 수 있도록 반드시 `overflow-y-auto overflow-x-hidden`을 유지한다. `overflow-hidden`으로 닫아 수강생 명단이나 긴 서식이 잘리는 현상을 원천 차단한다.

2. **모바일 출결 체크 원터치 순환 및 명시적 액션 바 보존**:
   - 모바일 학생 출석부 카드에서는 터치 편의성을 극대화하기 위해 출결 마크 버튼을 탭했을 때 `미체크(·) -> 출석(○) -> 지각/개별(△) -> 결석(×)` 순으로 즉각 1터치 순환 토글되도록 지원하고, 세부 드롭다운 메뉴도 함께 제공한다.
   - 모바일 회차 바 상단에는 '전원출석', '공유', '인쇄' 3대 핵심 제어 버튼을 아이콘 단독이 아닌 텍스트 라벨과 함께 눈에 띄게 배치하여 기능 접근성을 보장한다.

3. **모바일 목록 하단 드롭다운 방향 제어 (위쪽 표출 `dropUp`)**:
   - 모바일 출석부 등 긴 목록의 하단 항목(마지막 2개 이상 및 뷰포트 하단 공간 220px 미만)에서는 출결 메뉴나 드롭다운 팝업이 화면 아래(네비게이션 바 뒤)로 잘려 나가지 않도록 반드시 `bottom-full mb-1` 방향으로 위로 열리도록 제어한다.

4. **교원 및 관리자 전용 페이지의 `MainLayout` 표준 적용 (하단 네비 및 뒤로가기/홈 보장)**:
   - 학교 체육(`/teacher/pe`), 보건실, 방과후, 스쿨버스 등 교원 페이지는 반드시 공통 레이아웃인 `MainLayout`으로 감싸 렌더링한다.
   - 모바일 뷰에서 상단 헤더의 뒤로가기(`ArrowLeft`), 결재 홈(`Home`) 바로가기 및 최하단 5대 고정 네비게이션 바(`MobileBottomNav`: 방과후, 스쿨버스, 새문서, 학교체육, 학생계정)가 유실 없이 일관되게 제공되도록 보장한다.

## 방과후 버스 번호 요일별 독립 조회 규칙

1. **방과후 출석부/수강생 명단 버스 번호 요일별 실시간 매핑**:
   - 방과후 출석부(`AttendanceManagement.tsx`), 외부 강사 공유 출석부(`attendance/share/[courseId]/page.tsx`), 수강생 명단(`StudentManagement.tsx`) 등 방과후 버스 번호를 표출하는 모든 곳에서 학생 문서 단일 필드(`afterSchoolBusNo`)에 의존하지 않는다.
   - 반드시 `kisbus` DB의 `routes` 컬렉션을 실시간 구독(`onRoutesUpdate`)하고, `type === 'AfterSchool'` 노선에서 해당 학생(`studentId`)이 배정된 좌석(`seating`)을 찾아 `dayOfWeek`와 `busId`를 조합하여 `busesByDay: Record<string, string>` 요일별 버스 맵을 구성한다.
   - 출석부에서 특정 회차(날짜)를 선택했을 때 `activeDay.dateStr`에서 한글 요일(`(월)`, `(수)` 등)을 파싱하거나 `fullDate`에서 JS Date 요일 인덱스를 추출하여 영문 요일(`Monday`, `Wednesday`)로 변환한 뒤 `busesByDay[targetDayEn]`을 1순위로 조회한다.
   - 주 2회 이상 서로 다른 버스를 타는 학생(예: 월요일 39A호, 수요일 37호)은 수강생 명단에서 요일별 뱃지(`월: 39A호차`, `수: 37호차`)로 각각 표출하고, 출석부에서는 현재 선택된 회차 요일 버스만 단독 표출하여 혼선을 원천 차단한다.

2. **외부 강사 공유 출석부의 kisbus routes/buses 실시간 구독 필수**:
   - `attendance/share/[courseId]/page.tsx`는 외부 강사가 접근하는 공유 페이지이지만, 방과후 버스 번호 정확성을 위해 `onRoutesUpdate` 및 `onBusesUpdate` 구독을 포함해야 한다.
   - `getStudentBusNo` 호출 시 현재 선택된 `activeDay`를 6번째 인자(`targetDay`)로 전달하여 요일별 버스를 정확히 조회한다.

## 빌드 전략 및 TypeScript 오류 관리

1. **App Hosting 환경에서의 배포 전략**:
   - `firebase.json`에 `alwaysDeployFromSource: true`가 설정된 App Hosting 프로젝트에서는 `npm run build` 대신 `npm run typecheck`(tsc --noEmit)로 타입 오류만 전수 검사한 뒤 `git push`로 배포를 트리거한다.
   - Windows 환경에서 dev 서버가 실행 중일 때 `npm run build`를 실행하면 `.next\trace` 파일 락(EPERM) 또는 응답 없음이 발생할 수 있으므로, App Hosting 환경에서는 로컬 빌드 시도를 생략하고 typecheck → git push 배포 파이프라인을 표준으로 사용한다.

2. **TypeScript 기존 오류 우선 수정 원칙**:
   - 새 기능 추가 시 기존 파일에 이미 존재하는 TS 오류(타입 불일치, 미사용 프로퍼티 등)를 발견하면 함께 수정한다.
   - 특히 `modalStudent` 등 상태 타입이 컴포넌트 내부에서 확장되는 경우, 타입 선언에 선택적 필드(`busesByDay?: Record<string, string>`)를 추가하여 타입 안전성을 보장한다.

## 실무 전담 계정(직책: '담당')의 진입 경로 격리 및 리다이렉트 보장 규칙
1. **전담 계정 로그인 리다이렉트 및 관리자 지정 연동**:
   - 전자결재나 일반 교원 기능을 사용하지 않고 특정 현장 업무(스쿨버스 등)만 전담하는 계정은 `UserProfile.loginRedirectUrl` 필드를 통해 로그인 직후 전용 페이지로 즉시 자동 이동시킨다.
   - 시스템 설정의 교직원 관리 탭에서 직책이 '담당'인 계정을 추가하거나 편집할 때, 로그인 후 이동할 목적지 페이지(`/admin/bus`, `/teacher/bus` 등)를 지정할 수 있는 UI를 필수로 제공한다.
   - `bus@kshcm.net` 등 표준 시스템 전담 계정은 DB 상에 명시적 URL 필드가 누락되어 있더라도 로그인 시 기본 목적지(`/admin/bus`)로 자동 보정 및 저장하여 사용자 조작 누락에 따른 혼선을 원천 방지한다.
2. **직책 '담당'의 사이드바 원천 비활성화 및 접힘 상태 디폴트 고정**:
   - 직책이 '담당'(`profile?.role === '담당'`)인 사용자는 전자결재 사이드바를 전혀 사용하지 않으므로, `SidebarProvider`에서 로컬스토리지 저장 여부와 무관하게 `isSidebarOpen: false`를 디폴트로 고정하고 토글 기능을 차단한다.
   - 상단 `AppHeader`에서도 담당 직책 사용자에게는 사이드바 열기/접기 버튼을 렌더링하지 않아 온전한 전용 화면 공간을 확보한다.

## 스쿨버스 관리자 레이아웃 스크롤 컨테이너 및 헤더 Sticky 고정 규칙
1. **내부 스크롤 격리 및 탭/필터 헤더 무이탈 고정 (`sticky top-0`)**:
   - 스쿨버스 관리자(`/admin/bus`) 등 대량의 학생 카드와 좌석표를 스크롤하는 대형 관리자 화면은 `MainLayout`에 `contentClassName="p-0 h-full overflow-y-auto"`를 부여하여 상단 스쿨버스 헤더 아래 본문 영역이 독립 스크롤 컨테이너가 되도록 격리한다.
   - 내부의 버스 탭 및 요일/경로/정류장 필터 래퍼는 `sticky top-0`으로 설정하여, 학생 카드나 미배정 목록을 아래로 길게 스크롤하더라도 상단 헤더 및 필터 바가 뷰포트 밖으로 밀려 올라가거나 해제되지 않고 상단에 완벽히 고정되도록 보장한다.

## 대시보드 주요 업무 바로가기 교직원 역할별 격리 및 동적 프리셋 규칙
1. **역할/담당 기반 선택 목록 엄격 격리**:
   - 대시보드(`/inbox`) 상단 주요 업무 바로가기 및 설정 모달(`MajorTasksModal`)에서는 모든 교사에게 전체 업무나 특정 업무를 무조건 나열하지 않는다.
   - 반드시 로그인한 교사의 실제 담당 직책/권한(담임, 방과후 강좌, 스쿨버스 탑승/관리, 체육, 보건, 복무 등)에 해당하는 업무(`availableTasks`)만 선택 후보로 제공하여, 본인의 업무가 아닌 기능이 노출되거나 선택되는 것을 원천 차단한다.
2. **계정별 로컬 저장소 격리 및 하드코딩 프리셋 배제**:
   - 주요 업무 바로가기 설정은 브라우저 공용 키가 아닌 사용자 이메일별 고유 키(`kis_dashboard_major_tasks_v2_${email}`)로 분리 저장한다.
   - 특정 업무(방과후/스쿨버스 등)를 일괄 기본값으로 강제하지 않으며, 저장된 값이 없을 때는 해당 사용자가 실제로 담당하고 있는 업무 중 상위 최대 3개를 기본값으로 자동 산출한다.

## 방과후 및 스쿨버스 '강사' 직책의 접근 권한 및 데이터 엄격 격리 규칙
1. **직책 '강사'의 접근 범위 한정 및 라우팅 격리**:
   - 교직원 직책 중 '강사'(`role === '강사'`)는 오직 **본인의 방과후 출석부(`/teacher/afterschool`)**와 **스쿨버스 탑승 관리(`/teacher/bus`)**에만 접근 권한을 갖는다.
   - 전자결재(`/inbox`, `/sent`, `/pending` 등), 교원 서비스(복무, 초과근무, 보결), 학생 계정, 학교 체육, 보건 등 타 업무 및 기안 작성(`/new`) 화면은 사이드바, 모바일 하단바, 레이아웃(`(app)/layout.tsx`, `MainLayout`) 레벨에서 원천 숨김 및 차단한다.
   - 로그인 직후 및 비인가 경로 직접 URL 입력 시 기본 목적지인 `/teacher/afterschool`로 자동 리다이렉트한다.
2. **방과후 강좌 및 출석부의 본인 담당 강좌(`myOwnCourses`) 엄격 제한**:
   - 강사 직책인 사용자가 방과후 교사 페이지(`/teacher/afterschool`)에 진입할 경우, 일반 교사와 달리 강좌 목록(`teacherCourses`)에 전체 강좌 fallback을 절대 허용하지 않고 오직 본인이 주강사/추가강사(1~4)/보조강사로 배정된 강좌(`myOwnCourses`)만 반환한다.
   - 타 강사의 강좌, 수강생 명단 및 출석부 데이터가 노출되거나 수정되는 것을 원천 차단한다.

## 시스템 관리자 권한 토글 및 전담 계정 격리 규칙
1. **관리자 권한 강제 주입(Hardcoded Admin)의 슈퍼 관리자 한정 원칙**:
   - `auth-provider.tsx` 등 전역 인증 프로필 로더에서 `isAdmin: true`를 강제 주입하는 대상은 오직 최상위 슈퍼 관리자(`beside1s@kshcm.net`)로 한정한다.
   - 현장 전담 계정(`bus@kshcm.net` 등)에 `isHardcodedAdmin = true`를 부여하여 관리자가 시스템 설정에서 관리자 토글 스위치를 꺼도 다시 켜지게 만드는 하드코딩을 원천 차단한다.
2. **관리자 토글 변경 시 세션 상태 실시간 동기화**:
   - 시스템 설정의 사용자 관리 탭에서 특정 계정의 `isAdmin` 스위치를 변경할 때, 현재 로그인한 사용자 본인의 계정이면 `updateProfile({ isAdmin: checked })`를 즉시 호출하여 헤더의 설정 톱니바퀴 아이콘 및 관리자 메뉴가 페이지 새로고침 없이 즉각 동기화되도록 보장한다.
   - 설정 모달 열림 시(`isOpen === true`) 사용자 목록(`fetchUsers(true)`)을 강제 무효화하여 최신 Firestore 값을 온전히 반영한다.

## 방과후 공식 출석부 인쇄 레이아웃 규칙

1. **30명 초과 시 2페이지 분리 원칙 (잘림 원천 차단)**:
   - `OfficialAttendanceSheet.tsx`는 `students.length > 30`(ROWS_PER_PAGE)일 때 자동으로 2페이지로 분리한다.
   - 1페이지(1~30번), 2페이지(31번~)에 동일한 제목 + 지도교사 영역을 렌더링하고, 일련번호는 연속으로 이어진다.
   - CSS: `attendance-page-block { height: 277mm; page-break-after: always }`, 마지막 블록은 `page-break-after: avoid`.
   - `overflow: hidden + max-height` 구속은 단일 페이지 전용 클래스(`attendance-page-single`)에만 적용하며, 다중 페이지 래퍼에는 `overflow: visible`을 적용한다.

2. **출석부 인쇄 시 React Portal 필수 사용 원칙**:
   - `OfficialAttendanceSheet`는 반드시 `createPortal(content, document.body)`로 body 직속 자식으로 렌더링한다.
   - 이유: admin UI 레이아웃이 `visibility: hidden`으로 숨겨도 공간을 점유하여 빈 페이지가 발생하고, `position: fixed`는 인쇄 시 모든 페이지에 반복 렌더링되기 때문.
   - Print CSS는 `body > *:not(.oas-modal-overlay) { display: none }`으로 admin UI 레이아웃 공간까지 완전 제거하고, 모달 오버레이는 `position: static`으로 정상 흐름에 배치하여 `page-break-after`가 동작하도록 한다.
   - SSR 가드: `const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []);`로 클라이언트 마운트 후에만 portal 렌더링.

3. **출석부 상단 여백은 콘텐츠 내부 패딩으로만 조정**:
   - `position: absolute; top: Nmm` 방식으로 인쇄 영역 전체를 이동하면 `max-height`와의 합산 초과로 하단 행이 잘린다.
   - 제목 div의 `pt-[Nmm]`으로 내부 여백만 조정하고, 박스 크기는 건드리지 않는다.

4. **지도교사 영역 정렬 및 서명 배치 표준**:
   - 제목: 가운데 정렬(`text-center`).
   - 지도교사: 우측 정렬(`justify-end`), 각 강사 이름 바로 옆에 도장(`OfficialSeal`)을 쌍으로 배치.
   - 보조강사 구분 없이 전체를 `allInstructors = [mainInstructor, ...assistantInstructors]`로 통합하여 단일 라인에 나열.

## 방과후 강사 출근부 다중 강사 레이아웃 규칙

1. **출근부 열 폭 비율 표준 (4인 서명 가로 배치 보장)**:
   - `OfficialWorkRegister.tsx`의 출근부 표 열 폭은 차시 9% · 날짜 13% · 서명 28% (합계 50%, 좌우 동일).
   - 서명 칸 28% = 190mm 기준 53.2mm → 11mm 도장 4개 + 간격 4.5mm = 48.5mm → 한 줄 배치 가능.
   - 차시/날짜 폰트는 13px, 행 높이는 44px(11.6mm)로 유지하여 11mm 도장이 행을 넘치지 않도록 한다.

2. **강사 4명 이상 시 메타 정보 표 이름 2명씩 줄바꿈**:
   - `강사` 셀에서 `allCourseInstructors.length >= 4`이면 `slice(0,2)` 1행 + `slice(2)` 2행으로 분리 렌더링.
   - 3명 이하는 기존대로 한 줄 표기.

3. **`WorkRegisterSeal` 크기 표준**:
   - 이미지 서명: `maxWidth: 12mm, maxHeight: 11mm`.
   - 원형 직인: `width/height: 11mm`.
   - 출근부 행 높이 44px 기준 내에서 4인이 가로로 배치되도록 설계.

## 방과후 출석부 출결 기본값 및 자동화 규칙
1. **출결 상태 기본 '출석(○)' 자동 지정 및 전원출석 버튼 배제 원칙**:
   - 출석부 진입 시 대부분의 학생이 정상 출석하는 학교 현장 특성을 반영하여, 기록이 없는 학생은 기본 '출석(○)' 상태로 자동 초기화한다.
   - 단, 기존에 이미 저장된 출결 기록(결석, 지각, 조퇴, 개별하교, 기존 출석)은 절대 덮어쓰지 않고 완벽히 보존한다(`hasAnyRecord` 검증 후 미기록 회차만 초기화).
   - 기본값이 출석이므로 상단 '전원출석' 버튼은 UI에서 영구 제거하고, 교사는 결석/조퇴/개별하교 등 예외 학생만 원클릭/드롭다운으로 변경한다.

## 학급 목록 정렬 및 안전 비교 규칙
1. **학급 키 오름차순 정렬 및 유치원·특수 학년 안전 비교 원칙**:
   - 학급 선택 드롭다운(`availableClassKeys`, `myHomeroomKeys`) 정렬 시 단순 `Number(grade)` 파싱을 사용하면 유치원(예: `유치원국화반-1`) 등의 비숫자 문자열이 `NaN`을 반환하여 정렬 비교가 붕괴되고 5~6학년 사이에 유치원이 끼어들며 전체 정렬이 왜곡된다.
   - 반드시 유치원 계열은 `gradeRank = 0`(초등 1학년 앞)으로 가중치를 부여하고, 초등 1~6학년은 `1~6`, 중고등은 `7~12`로 명시적 순위를 부여하여 오름차순 정렬한다.

## 스쿨버스 담당 교사 배정 구조 (4대 체계 및 등하교 단일 배정 원칙)
1. **스쿨버스 담당 교사 4대 배정 카테고리**:
   - 스쿨버스 담당 교사는 `등하교(commute)`, `방과후(afterSchool)`, `토요일(saturday)`, `등교지도(morningGate)` 4개 카테고리로만 배정 및 운영된다.
2. **등하교 단일 배정 및 하교(Afternoon) 정본 원칙**:
   - 등교와 하교는 별개의 버스로 배정되지 않으며 동일한 버스를 동일 교사가 담당한다. 따라서 교사용 페이지(`/teacher/bus`)와 관리자 페이지(`/admin/bus`)에서 등교와 하교를 쪼개지 않고 '등하교' 단일 버스로 통합 관리 및 표출한다.
   - Firestore DB 상 과거 잔재로 인해 `Morning` 노선과 `Afternoon` 노선 간 교사 배정 데이터가 불일치할 경우, 항상 하교(`Afternoon`) 노선의 배정 데이터를 정본(Single Source of Truth)으로 삼아 등교 노선에 100% 동기화하며, 교사 정보 모달 및 UI 로딩 시에도 `Afternoon` 데이터를 최우선으로 바인딩한다.

## 학생 계정 관리 및 3대 컬렉션(users, master_students, kisbus) 완결 동기화 규칙
1. **학생 계정 `users` 문서 ID 표준화 (이메일 기반 고유 키)**:
   - `masterStudentService` 등에서 개별 학생 계정을 생성할 때 `doc(collection(db, 'users'))` 난수 ID 생성을 원천 차단한다.
   - 반드시 소문자 학생 이메일(`cleanEmail.toLowerCase()`)을 문서 ID로 지정하여 생성하며, `email, name, studentName, displayName, grade, studentGrade, class, studentClass, number, studentNumber, gender, role: 'student'` 등 일관된 필드명을 동시 저장하여 시스템 설정 및 디렉토리 조회 시 누락 및 미배정 현상을 방지한다.

2. **학생 성별(`gender`) 변경 보장 및 스쿨버스·체육(PAPS) 다방향 실시간 동기화**:
   - 학생의 성별 정보(`'Male' | 'Female'`)는 스쿨버스 좌석 배치 및 학교체육(PAPS) 남/여 기준표 판정에 핵심적인 데이터이므로, '개별 계정 추가' 및 '학생 정보 수정' 모달 모두에 성별 선택 UI(`Select`)를 필수로 제공한다.
   - 학생 정보 수정(`updateMasterStudent`) 시 `master_students` 컬렉션뿐만 아니라 메인 `users` 컬렉션과 스쿨버스 DB(`kisbus.students`)의 `gender` 필드를 동시에 양방향 동기화하여 데이터 불일치를 원천 차단한다.

3. **담임 출결 등록 시 스쿨버스 및 방과후 완결 연동 (복합 키 역추적)**:
   - 담임 교사가 결석/조퇴/인정결석을 등록할 때, 포털 학생 ID에만 의존하지 않고 스쿨버스 DB(`kisbus.students`)를 `이름 + 학년 + 반` 복합 키 및 학생 이메일로 반드시 역추적하여 실제 버스 학생 ID를 도출한다.
   - 역추적된 학생은 당일 스쿨버스 운행 노선의 `notBoarding` 목록에 자동 등록되어야 하며, 방과후 수강 명단(`afterschool_enrollments`)도 동일하게 조회하여 당일 방과후 출석부(`attendance`)에 자동으로 결석 사유가 동기화되어야 한다.

4. **방과후 수강 신청 시 학생 프로필 영구 보존 및 다자녀 오염 방지**:
   - 방과후 수강 신청 트랜잭션(`runAfterschoolEnrollmentTransaction`) 실행 시 학생의 학년(`grade`), 반(`classNum`), 번호(`studentNum`), 학생 이메일(`studentEmail`), 버스 신청 여부(`needsBus`)를 수강 문서에 반드시 영구 저장한다.
   - 학부모가 다자녀를 대리 신청할 때 부모 프로필이나 타 자녀의 정보로 덮어씌워지지 않도록 각 자녀별 독립 프로필을 명시적으로 바인딩한다.

5. **스쿨버스 미등록 학생의 방과후 하교 버스 자동 프로비저닝**:
   - 정규 등하교 버스를 이용하지 않는 학생이 방과후 하교 버스 탑승을 희망(`needsBus === true` 또는 `kisbusNo !== '-'`)한 경우, 스쿨버스 관리자 배정 탭에서 누락되지 않도록 `kisbus.students`에 기본 프로필을 안전하게 자동 생성(프로비저닝)하여 관리자가 즉시 좌석을 배정할 수 있도록 보장한다.

6. **통합 학생 마스터 계정 엑셀 일괄 등록 표준 양식 및 방과후/버스 연동 미리보기 검증**:
   - 통합 학생 마스터 엑셀 일괄 등록 시 표준 양식(`학년`, `반`, `번호`, `이름`, `영문이름(선택)`, `성별`, `계정(이메일)`, `학부모 연락처(선택)`)을 필수 제공한다.
   - 영문이름, 학부모 연락처 등 선택 항목이 비어있어도 정상 등록되어야 한다.
   - 엑셀 업로드 시 즉각 DB 반영 전 `학년 + 반 + 이름` 복합 키로 `afterschool_enrollments` 및 `kisbus.students`와 상호 대조하여 방과후 수강 강좌 및 스쿨버스 노선 연동 현황(연동 정보 또는 '없음')을 미리보기 테이블에 표시한다.
   - 계정 이메일 유효성 검사(`isStudentEmail`)를 통해 오류 행을 사전에 식별하고, 관리자가 미리보기 확인 후 최종 일괄 등록할 수 있도록 보장한다.

7. **학생 영문 이름(`nameEn`) 관리 및 이메일 기반 자동 추출과 영문 출석부 표출 보장**:
   - 학생 정보 수정 모달(학급 담임 및 관리자 마스터)과 신규 학생 등록 모달에 '학생 영문 이름(`nameEn`)' 입력 필드를 필수로 제공한다.
   - 기존 등록된 영문 이름이 있으면 해당 값을 표시하고, 영문 이름이 없는 경우에는 학생 이메일 계정(예: `2022kangsoobin@kshcm.net`)에서 입학년도(숫자 4자리)를 제외한 영문 문자열(`kangsoobin`)을 자동 추출(`extractEnglishNameFromEmail`)하여 기본값으로 입력 및 바인딩한다.
   - 언어 설정을 영문(`en`)으로 전환했을 때나 외부 강사용 공유 출석부에서 외국인 교사가 학생 이름을 읽지 못해 출결 체크에 지장이 생기지 않도록, 학생 이름을 영문 이름(`nameEn` 또는 추출된 영문명)으로 최우선 렌더링하고 한글 이름을 괄호로 병기한다.

8. **담임 교사 학생 정보 탭의 하교 버스 요일별 표시 및 직접 해제 기능 표준화**:
   - 담임 교사 페이지(`/teacher/homeroom`)의 학생 정보 확인 탭에서 스쿨버스 노선 컬럼은 `kisbus routes` 컬렉션(`type === 'Afternoon'`)을 실시간 구독(`onRoutesUpdate`)하여 **학생별 요일별 하교 버스 맵**을 계산한다.
   - 요일별로 버스가 동일하더라도 모든 배정 요일을 개별 뱃지(`[하교 월] 18호`, `[하교 화] 18호` 등)로 항상 표시한다. 조건부 통합 표시를 하면 안 된다.
   - 하교 버스 뱃지는 클릭 가능한 상태(`cursor-pointer`, hover 색상 변경)로 표시하며, 클릭 시 "유지/해제" 확인 Dialog를 열어 담임 교사가 해당 요일 하교 버스 탑승을 직접 미배정 처리(`unassignStudentFromAllRoutes(studentId, ['Afternoon'], dayOfWeek)`)할 수 있도록 한다.
   - 해제 즉시 `kisbus routes` 컬렉션이 업데이트되어 스쿨버스 관리자 페이지·교사 페이지와 실시간 공유된다.
   - 등교 버스는 담임이 조정하지 않으므로 해제 기능을 제공하지 않고, 방과후 버스도 방과후 전담 페이지에서 관리하므로 담임 페이지에서는 표시만 한다.

9. **방과후 강좌 강사진 최대 6명 동적 확장 및 UI 최적화 규칙**:
   - 방과후 강좌 정보 마스터 수정(`AdminPanel`) 및 강좌 관리(`CourseManagement`)에서 다수 강사 참여 강좌(오케스트라 등)를 원활히 지원하기 위해 강사진을 최대 6명(`instructorName`, `instructor2`~`instructor6`, `assistantTeachers`)까지 등록 및 관리할 수 있도록 확장한다.
   - 수정 모달 진입 시 불필요하게 팝업창이 길어지지 않도록 기본 2명(`강사 1(주강사)`, `강사 2(보조강사)`)만 노출하고, 기존에 등록된 강사 수에 맞춰 슬롯을 동적으로 펼친다.
   - '강사 추가' 버튼을 통해 3~6번째 강사 슬롯을 순차적으로 추가할 수 있으며, 추가된 슬롯에는 개별 '삭제' 버튼을 제공하여 강사명 초기화 및 슬롯 축소를 지원한다.
   - 보조강사 배정 모달(`CourseManagement`)에서도 최대 5명(주강사 포함 총 6명)까지 배정 및 저장(`instructor2~6`)할 수 있도록 상호 연동한다.
   - 공식 출석부(`OfficialAttendanceSheet`), 강사 출근부(`OfficialWorkRegister`), 사이드바 및 결재함 본인 강좌 필터링에서도 6명 전체 강사를 누락 없이 검증 및 표출한다.


## UI 컴포넌트 Radix Dialog + Popover 포커스 트랩 원칙

1. **Dialog 내부 Popover/Combobox 포커스 트랩 해결**:
   - Radix UI `Dialog` 컴포넌트 내부에서 `Popover` 기반 컴포넌트(Combobox, Command, DatePicker 등)를 사용할 때 Dialog의 포커스 트랩(focus trap)으로 인해 팝오버가 열리지 않는 현상이 발생할 수 있다.
   - 이를 해결하려면 `Popover` 컴포넌트에 `modal={true}` prop을 명시하여 팝오버가 독립적인 포커스 레이어에서 동작하도록 강제한다.
   - `Combobox` 등 공용 컴포넌트에는 `modal?: boolean` prop을 추가하고, Dialog 내부에서 사용 시 `modal={true}`로 전달한다.

## 스쿨버스 신규 학생 추가 원칙

1. **통합 계정 명단 기반 학생 추가 필수**:
   - 스쿨버스 관리자(`/admin/bus`)에서 새 학생을 스쿨버스 명단에 추가할 때, 직접 정보를 수기 입력하는 방식을 허용하지 않는다.
   - 반드시 통합 학생 마스터(`master_students`) 계정 명단에서 이름/이메일 검색 및 학년 필터로 학생을 찾아 선택한 뒤 kisbus `students` 컬렉션에 연동 등록하는 방식을 사용한다.
   - 이미 kisbus에 등록된 학생(`studentEmail` 또는 `이름+학년+반` 복합 키 중복)은 검색 결과에서 자동 제외하여 중복 등록을 원천 차단한다.
   - 학생 추가 시 `studentEmail`을 포함하여 저장함으로써 이후 통합 계정과의 자동 매핑이 가능하도록 보장한다.

