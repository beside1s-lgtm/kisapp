# Workspace Rules (KISAPP)

## Firebase 프로젝트 격리 및 배포 안전 규칙
1. **프로젝트 ID 바인딩 확인**:
   - Firebase 관련 작업(보안 규칙 배포, Functions, Firestore 조회 등)을 수행할 때는 절대 MCP의 기본 세션 값에 의존하지 않는다.
   - 작업 시작 전 반드시 현재 워크스페이스의 `.firebaserc` 또는 `firebase.json`에서 정확한 `projectId`를 확인하고, `firebase_update_environment`를 통해 현재 프로젝트 디렉토리와 `active_project`를 1:1로 일치시킨다.
2. **배포 시 명시적 프로젝트 플래그 필수**:
   - CLI 명령어로 Firebase 배포(`deploy`)를 실행할 때는 반드시 `--project <프로젝트ID>` 플래그를 명시하여 타 프로젝트로의 오배포를 원천 차단한다.
3. **배포 승인 규칙**:
   - 사용자의 명시적인 배포 승인("배포해", "배포해줘")이 있을 때만 원격 푸시 및 배포 명령을 실행한다.

## 스쿨버스 및 방과후 도메인 로직 규칙
1. **경로 및 요일 엄격 필터링**:
   - 스쿨버스 목적지 파악, 미편성 목적지 계산, 탑승 학생 필터링 시 반드시 선택된 **경로(`routeType`: 등교/하교/방과후)**와 **요일(`dayOfWeek`)**에 실제로 탑승하는 학생만 대상으로 추출한다.
   - 특히 `AfterSchool`(방과후)의 경우, 해당 요일에 방과후 수업을 실제로 수강하거나 방과후 목적지가 등록된 학생만 대상으로 해야 하며, 수업이 없는 요일(금요일 등)에는 대상자가 0명으로 정확히 처리되어야 한다. 일반 하교 목적지(`afternoonDestinationId`)를 임의 fallback으로 사용하여 대량의 오탐 목적지가 발생하는 것을 원천 차단한다.
   - 해당 경로/요일의 노선(stops)에 이미 등록되어 있는 목적지(ID 및 명칭 매칭)는 미편성 목록에서 완벽하게 제외한다.

2. **관계형 참조 필드 유효 데이터 검증 (Orphan ID 누출 방지)**:
   - 버스 번호, 목적지 등 참조 필드를 렌더링할 때는 유효성 검증 없이 Firestore 문서 ID를 fallback으로 화면에 노출하지 않는다.
   - 반드시 실제로 등록된 유효 데이터(`b.name` 존재)만 필터링하고 번호순으로 정렬하여 사용자 화면에 유령 ID가 노출되는 것을 원천 차단한다.

## 학부모 신청서 및 공문서 출력/결재 서식 규칙
1. **결재란 직책 슬롯 표준화**:
   - 상단 결재란의 헤더 직책명은 결재자의 세부 보직명(`교무부장`, `학년부장`, `1반 담임` 등)을 직접 노출하지 않고, 반드시 표준 슬롯명(`['담임', '부장', '교감', '교장']`) 2글자 고정으로 렌더링하여 결재란 너비 붕괴 및 텍스트 쏠림을 방지한다.

2. **원본대조필 직인 노출 격리 (학부모 사본 전용)**:
   - 교원/관리자 결재문서함(`/documents/[id]`)은 학교 보관용 원본 문서이므로 하단 여백의 '원본대조필' 직인을 절대 표시하지 않는다.
   - 원본대조필 직인은 오직 학부모 포털(`/parents/documents/[id]`, 인쇄 및 PDF 다운로드)에서 결재가 완료(`approved`)된 문서에 한해 하단 우측 여백에 날인한다.

3. **A4 1페이지 규격 및 인라인 테두리 보존**:
   - 공문서 및 신청서 양식은 브라우저 인쇄와 PDF 변환 시 테두리가 유실되지 않도록 모든 `table`, `th`, `td`에 인라인 스타일(`border: '1px solid #000000'`, `borderCollapse: 'collapse'`)을 명시한다.
   - 1페이지 독립 서식(서식 1 신청서, 서식 2 결과보고서, 서식 3 결석계)은 A4 규격(297mm) 내에 완벽히 수납되도록 행 높이와 여백을 정밀 계산하여 하단 서약란 및 직인이 다음 페이지로 밀리는 것을 원천 차단한다.

4. **PDF 변환 엔진 및 화면 1:1 캡처 보존**:
   - A4 서식의 PDF 다운로드 변환 시 가상 뷰포트 재렌더링으로 왜곡을 유발하는 `html2canvas` 대신, 브라우저의 실제 렌더링 픽셀을 그대로 캡처하는 `html-to-image`(`toPng`, `pixelRatio: 2.5`, `cacheBust: true`, `width: el.offsetWidth, height: el.offsetHeight`)를 사용한다.
   - 사용자가 화면에서 보고 있는 렌더링 결과와 1픽셀의 오차도 없이 1:1로 일치하도록 보장한다.

5. **서명란 및 인명/직함 라벨 줄바꿈 원천 차단 (`whitespace-nowrap`)**:
   - 서약 및 서명 영역의 '보호자 :', '학부모 :', '학생 :' 등 라벨과 기안자/보호자명, 직인 슬롯은 폭 축소 시 콜론(`:`)이나 텍스트가 다음 줄로 떨어지지 않도록 반드시 `whitespace-nowrap` 및 인라인 플렉스 컨테이너(`inline-flex items-center whitespace-nowrap`)로 묶어 렌더링한다.

6. **테이블 셀 수직 중앙 정렬 및 2mm 여백 표준화**:
   - 모든 단일행 테이블 셀(`th`, `td`)은 텍스트가 셀 하단에 붙지 않도록 `display: flex; align-items: center;` 컨테이너 및 `padding: '2mm'`(또는 내부 `min-h` 및 `leading-tight`)를 적용하여 완벽한 수직 중앙 정렬과 시각적 여백을 유지한다.
