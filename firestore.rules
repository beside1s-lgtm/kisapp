rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 로그인한 사용자만 읽고 쓸 수 있음
      allow read, write: if request.auth != null;
    }
  }
}
